{
  "name": "crypto-portfolio-tracker",
  "type": "module",
  "private": true,
  "workspaces": ["layout-builder", "apps-script"],
  "scripts": {
    "deploy": "bun run --filter apps-script deploy",
    "build:apps-script": "bun run --filter apps-script build"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
