---
phase: quick-260621-m70
plan: 01
subsystem: infra
tags: [layout-builder, google-sheets, conditional-format, error-handling, regression-test]

# Dependency graph
requires:
  - phase: 02-sheet-layout
    provides: "--update structural re-apply + conditional-format pre-clear pipeline"
provides:
  - "Broadened, exported isNoConditionalRuleAtIndexError matcher that recognizes the real Sheets API delete-error message"
  - "Offline regression test locking both accepted phrasings and rejecting unrelated errors"
affects: [layout-builder, --update]

# Tech tracking
tech-stack:
  added: []
  patterns: ["Error-tolerance predicate matched on stable API vocabulary (not bare substrings), unit-tested offline"]

key-files:
  created: []
  modified:
    - layout-builder/src/index.js
    - layout-builder/src/index.test.js

key-decisions:
  - "Match the real message via the stable surrounding tokens (No conditional format on sheet:.*at index:) rather than literal sheet/index numbers"
  - "Keep the matcher anchored on conditional-format-delete vocabulary so auth/missing-sheet/malformed errors still surface loudly"

patterns-established:
  - "Two-alternative case-insensitive regex tolerating both legacy and live API phrasings without narrowing"

requirements-completed: [WR-01]

# Metrics
status: complete
duration: 5min
completed: 2026-06-21
---

# Quick Task 260621-m70: Fix --update Conditional-Format Pre-Clear Matcher Summary

**Broadened the `--update` conditional-format pre-clear tolerance so it recognizes the REAL live Sheets API message ("No conditional format on sheet: \<id\> at index: \<n\>"), unblocking the rename + structural re-apply that previously aborted.**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-06-21
- **Completed:** 2026-06-21
- **Tasks:** 1 (TDD)
- **Files modified:** 2

## Accomplishments

- Exported and broadened `isNoConditionalRuleAtIndexError(err)` in `layout-builder/src/index.js` to match BOTH the real live Sheets API delete-error phrasing and the legacy phrasing, via a single case-insensitive two-alternative regex.
- Kept the matcher anchored on conditional-format-delete vocabulary so auth (`PERMISSION_DENIED`), missing-sheet, and malformed-request errors still surface loudly — no bare `index` substring.
- Updated the WR-01 comment block to cite the real observed message verbatim, noting it was confirmed live against the spreadsheet.
- Added an offline regression test (`layout-builder/src/index.test.js`) asserting both phrasings are accepted and unrelated/auth/missing-sheet errors are rejected.
- Left the descending delete-index arrays, pre-clear request shapes, DCA Log data-region safety logic, and both try/catch warn messages untouched (call sites at lines 203/216 unchanged).

## Task Commits

TDD task (RED test + GREEN implementation committed together, as the test imports the newly exported symbol):

1. **Task 1: Broaden + export conditional-format-delete matcher and lock with regression test** - `670ac15` (fix)

_Note: The matcher is the unit under test; test and implementation were committed atomically since the new export must exist for the test module to load._

## Files Created/Modified

- `layout-builder/src/index.js` - Exported and broadened `isNoConditionalRuleAtIndexError`; updated WR-01 comment block to cite the real message.
- `layout-builder/src/index.test.js` - Added 4 regression tests for the matcher (real phrasing, legacy phrasing, auth rejection, missing-sheet/malformed rejection); imported the matcher alongside `resolveLogTabRequests`.

## Decisions Made

- Matched the live message via stable surrounding tokens (`No conditional format on sheet:.*at index:`) rather than the variable sheet id / index numbers, so any sheet/index pair is tolerated.
- Used two explicit regex alternatives (legacy + live) instead of one merged pattern, to keep each branch readable and intentionally scoped.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None. RED confirmed the failing state (export not found / real message unmatched); GREEN passed after the broadened export.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- `node --env-file=.env src/index.js --update` can now run end-to-end past the tolerated pre-clear delete (rename legacy "DCA Log" tab + re-apply structure) without aborting on the real Sheets API error.
- Full layout-builder offline suite green: 55 pass / 0 fail across 4 files.

## Self-Check: PASSED

- Commit `670ac15` found in git log.
- `export function isNoConditionalRuleAtIndexError` present in `layout-builder/src/index.js`.
- Real message token `No conditional format on sheet` present in `layout-builder/src/index.js`.
- `rtk bun test` (full suite) green: 55 pass / 0 fail.

---
*Phase: quick-260621-m70*
*Completed: 2026-06-21*
