---
phase: quick-260621-m70
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - layout-builder/src/index.js
  - layout-builder/src/index.test.js
autonomous: true
requirements: [WR-01]

must_haves:
  truths:
    - "isNoConditionalRuleAtIndexError() returns true for the real Sheets API message 'No conditional format on sheet: 1 at index: 2'"
    - "isNoConditionalRuleAtIndexError() still returns true for the legacy 'No conditional format rule found at index 0' message"
    - "isNoConditionalRuleAtIndexError() returns false for unrelated errors (403 / PERMISSION_DENIED)"
    - "bun test passes green for the layout-builder offline suite"
  artifacts:
    - path: "layout-builder/src/index.js"
      provides: "Broadened, exported conditional-format-delete error matcher"
      contains: "export function isNoConditionalRuleAtIndexError"
    - path: "layout-builder/src/index.test.js"
      provides: "Regression test asserting the matcher accepts both phrasings and rejects unrelated errors"
      contains: "isNoConditionalRuleAtIndexError"
  key_links:
    - from: "layout-builder/src/index.js runUpdate() pre-clear try/catch (Dashboard + log tab)"
      to: "isNoConditionalRuleAtIndexError"
      via: "guard on caught error before re-throw"
      pattern: "isNoConditionalRuleAtIndexError\\(err\\)"
---

<objective>
Fix the `--update` conditional-format pre-clear error tolerance in `layout-builder/src/index.js` so the broadened matcher recognizes the REAL Sheets API "delete a non-existent conditional-format rule" message, not just the legacy phrasing it currently checks.

The pre-clear step best-effort deletes managed conditional-format rules in descending index order before the structural batch re-adds them. The tolerance check `isNoConditionalRuleAtIndexError()` (index.js:137-140) only matches `"No conditional format rule found at index"`, but the live Sheets API actually returns:

    Invalid requests[0].deleteConditionalFormatRule: No conditional format on sheet: 1 at index: 2

That string does not match the current regex, so the error is re-thrown and `--update` aborts before the rename + structural re-apply. Confirmed live: both tabs currently have 0 rules and the log tab is still legacy-titled "DCA Log" — NO `--update` has landed since Phase 2. The single matcher guards BOTH the Dashboard pre-clear (index.js:200-208) and the log-tab pre-clear (index.js:213-221), so fixing it unblocks both.

Purpose: Restore the ability to run `node --env-file=.env src/index.js --update` end-to-end (rename legacy log tab + re-apply structure) without dying on the tolerated pre-clear delete error.
Output: A broadened+exported `isNoConditionalRuleAtIndexError` predicate, an updated comment block citing the real message, and an offline regression test that locks in both accepted phrasings and a rejected unrelated error.
</objective>

<execution_context>
@/home/bagashiz/Projects/crypto-portfolio-tracker/.claude/gsd-core/workflows/execute-plan.md
@/home/bagashiz/Projects/crypto-portfolio-tracker/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@layout-builder/src/index.js
@layout-builder/src/index.test.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Broaden + export the conditional-format-delete error matcher and lock it with a regression test</name>
  <files>layout-builder/src/index.js, layout-builder/src/index.test.js</files>
  <behavior>
    - Accepts the real Sheets API message: "Invalid requests[0].deleteConditionalFormatRule: No conditional format on sheet: 1 at index: 2" -> true
    - Still accepts the legacy message: "No conditional format rule found at index 0" -> true (backward compatibility, do not narrow)
    - Rejects an unrelated error such as a 403 with message "PERMISSION_DENIED: The caller does not have permission" -> false
    - Rejects a missing-sheet / malformed-request style error (e.g. "Unable to parse range" or "No sheet with id: 99") -> false
  </behavior>
  <action>
    In layout-builder/src/index.js: change the predicate at lines 137-140 to `export function isNoConditionalRuleAtIndexError(err)` (it is consumed by runUpdate at the two pre-clear call sites, lines 203 and 216 — keep those call sites unchanged). Broaden the regex so it matches BOTH phrasings without swallowing unrelated errors. Keep the original alternative `No conditional format rule found at index` AND add the real wording `No conditional format on sheet:` followed by `at index:` (the sheet id and index numbers vary, so match the stable surrounding tokens, not the literal "1"/"2"). Use a single case-insensitive regex with two alternatives, e.g. one branch matching `/no conditional format rule found at index/i` and one matching `/no conditional format on sheet:.*at index:/i` — anchored on the deleteConditionalFormatRule vocabulary so a generic 403/auth/missing-sheet error cannot match. Do NOT add a bare substring like "index" that could match unrelated messages. Update the comment block at lines 132-140 (WR-01) to cite the REAL observed message verbatim ("No conditional format on sheet: <id> at index: <n>") alongside the legacy phrasing, and note it was confirmed live against the spreadsheet. Do NOT touch the descending delete-index arrays, the pre-clear request shapes, the DCA Log data-region safety logic, or the two try/catch warn messages. In layout-builder/src/index.test.js: add a regression test block importing `isNoConditionalRuleAtIndexError` from `./index.js` (alongside the existing `resolveLogTabRequests` import). Follow the existing offline `import "./testEnv.js"` + `bun:test` style. Add cases asserting: accepts an Error whose message is the real `"Invalid requests[0].deleteConditionalFormatRule: No conditional format on sheet: 1 at index: 2"`, accepts the legacy `"No conditional format rule found at index 0"`, and REJECTS a `"PERMISSION_DENIED: ..."` error and a non-conditional-format error. Construct the inputs as `new Error(message)` (the predicate reads `err.message`). These tests must run offline with no live Sheets client.
  </action>
  <verify>
    <automated>cd layout-builder && rtk bun test src/index.test.js</automated>
  </verify>
  <done>`isNoConditionalRuleAtIndexError` is exported from index.js, matches both the real and legacy delete-error messages, rejects unrelated/auth errors, the comment block cites the real message, and `rtk bun test src/index.test.js` from layout-builder/ passes green.</done>
</task>

</tasks>

<verification>
- Run `cd layout-builder && rtk bun test` — the full layout-builder offline suite (index/dashboardSheet/dcaLog tests) passes green; no live Sheets client is required.
- Grep confirms the matcher is exported and matches both phrasings: `rtk grep "export function isNoConditionalRuleAtIndexError" layout-builder/src/index.js` and `rtk grep "No conditional format on sheet" layout-builder/src/index.js` both return a hit.
- Confirm no unintended change to the data-safety surface: `rtk git diff layout-builder/src/index.js` touches ONLY the predicate (lines ~132-140) and its `export` keyword — no edits to the descending delete-index arrays, pre-clear request builders, or the two try/catch warn blocks.
</verification>

<success_criteria>
- `isNoConditionalRuleAtIndexError("Invalid requests[0].deleteConditionalFormatRule: No conditional format on sheet: 1 at index: 2")` returns true.
- `isNoConditionalRuleAtIndexError("No conditional format rule found at index 0")` still returns true.
- `isNoConditionalRuleAtIndexError("PERMISSION_DENIED: ...")` returns false.
- The predicate is exported and imported by the regression test in index.test.js.
- The WR-01 comment block cites the real observed Sheets API message.
- `bun test` (from layout-builder/) is green.
- No change to DCA Log data-region safety logic, delete-index arrays, or pre-clear request shapes.
</success_criteria>

<output>
Create `.planning/quick/260621-m70-fix-update-conditional-format-pre-clear-/260621-m70-SUMMARY.md` when done
</output>
