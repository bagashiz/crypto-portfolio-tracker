---
phase: 02-layout-builder
plan: 03
type: execute
wave: 1
depends_on: ["02-01", "02-02"]
gap_closure: true
files_modified:
  - layout-builder/src/config.js
  - layout-builder/src/dcaLogSheet.js
  - layout-builder/src/dcaLogSheet.test.js
  - layout-builder/src/config.test.js
autonomous: true
requirements: [LAYOUT-02]
must_haves:
  truths:
    - "DATA_START_ROW is a fixed integer literal decided at build time, NOT derived from assets.length (its value does not change when the asset registry grows or shrinks)"
    - "After a one-line CONFIG-01 asset add/remove, re-running `--update` re-stamps the transaction header at the SAME fixed row as before — it never lands on a previously-existing DCA data row"
    - "Per-asset summary rows fill from the top up to assets.length; reserved-but-unused summary rows stay blank (label/format-only, no formulas, D-08); the transaction header sits at the fixed row below the reserved block"
    - "The DCA Log update request set provably never addresses any row at or below the fixed DATA_START_ROW, and this is asserted against a hard literal so a registry change that would move the boundary fails the suite loudly"
    - "Exceeding MAX_SUMMARY_ROWS (assets.length > MAX_SUMMARY_ROWS) fails loudly at build/update time rather than silently shifting the boundary"
  artifacts:
    - path: "layout-builder/src/config.js"
      provides: "MAX_SUMMARY_ROWS reservation + DATA_START_ROW as a fixed literal independent of assets.length"
      contains: "MAX_SUMMARY_ROWS"
    - path: "layout-builder/src/dcaLogSheet.js"
      provides: "Top-of-data band positioned from the FIXED boundary; per-asset summary rows fill top-down within the reserved block; loud guard when assets.length > MAX_SUMMARY_ROWS"
      exports: ["dcaLogBuildRequests", "dcaLogUpdateRequests"]
    - path: "layout-builder/src/config.test.js"
      provides: "Asserts DATA_START_ROW equals a hard literal AND mutating assets.length does not move the boundary"
    - path: "layout-builder/src/dcaLogSheet.test.js"
      provides: "Data-region-safety assertion anchored to the fixed literal (not a value re-derived from the registry)"
  key_links:
    - from: "layout-builder/src/dcaLogSheet.js"
      to: "layout-builder/src/config.js"
      via: "import DATA_START_ROW, MAX_SUMMARY_ROWS"
      pattern: "import.*config\\.js"
    - from: "layout-builder/src/dcaLogSheet.test.js"
      to: "layout-builder/src/config.js"
      via: "import DATA_START_ROW (asserted against hard literal)"
      pattern: "DATA_START_ROW"
---

<objective>
Close the single blocking verification gap (truth #4 / LAYOUT-02): `DATA_START_ROW = assets.length + 3` (config.js:44) FLOATS with the asset count, so the documented one-line CONFIG-01 asset add followed by `--update` re-stamps the transaction header directly onto the first existing DCA data row — overwriting real transactions with header text. This is the exact irreversible-data-loss class LAYOUT-02 exists to prevent. The existing data-safety test is blind to it because it imports the same derived `DATA_START_ROW`, so its assertion boundary moves in lockstep with the band.

Purpose: Make the DCA Log data-region boundary a FIXED absolute row decided at build time, never moving when the registry changes. The fix is to pin the boundary (D-05/D-06 mechanism unchanged — still "top-of-data band; `--update` never references the data region"), NOT to introduce read-detect-write logic.

Output: `config.js` rewired so `DATA_START_ROW` is a fixed literal backed by a `MAX_SUMMARY_ROWS` reservation; `dcaLogSheet.js` positions the band from the fixed boundary, fills per-asset summary rows top-down within the reserved block, leaves unused reserved rows blank, and fails loudly if `assets.length > MAX_SUMMARY_ROWS`; a new `config.test.js` asserting the boundary is a hard literal AND invariant under registry-length change; `dcaLogSheet.test.js` hardened so its data-region assertion is anchored to the fixed literal.
</objective>

<execution_context>
@/home/bagashiz/Projects/crypto-portfolio-tracker/.claude/gsd-core/workflows/execute-plan.md
@/home/bagashiz/Projects/crypto-portfolio-tracker/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-layout-builder/02-CONTEXT.md
@.planning/phases/02-layout-builder/02-VERIFICATION.md
@.planning/phases/02-layout-builder/02-PATTERNS.md
@.planning/phases/02-layout-builder/02-01-SUMMARY.md
@layout-builder/src/config.js
@layout-builder/src/dcaLogSheet.js
@layout-builder/src/dcaLogSheet.test.js
@assets.json
</context>

<artifacts_this_phase_produces>
New / changed symbols produced by THIS plan (consumed by Phase 5 SUMIF formulas and any future registry edit):
- `config.js` new export: `MAX_SUMMARY_ROWS` (integer reservation = fixed maximum per-asset summary capacity; chosen `20`).
- `config.js` changed export: `DATA_START_ROW` — now a FIXED integer literal (`23`), computed from `MAX_SUMMARY_ROWS` constants at module load with NO reference to `assets.length`. Layout: row 1 = summary header; rows 2..(1+MAX_SUMMARY_ROWS) = reserved summary block (rows 2-21); row (2+MAX_SUMMARY_ROWS) = transaction header (row 22); `DATA_START_ROW` = (3+MAX_SUMMARY_ROWS) = 23 = first data row.
- `dcaLogSheet.js` unchanged exports (`dcaLogBuildRequests`, `dcaLogUpdateRequests`) but now: band positioned from the fixed boundary; per-asset summary labels fill rows 2..(1+assets.length) top-down; reserved rows (1+assets.length+1)..(1+MAX_SUMMARY_ROWS) stay blank; transaction header at the fixed row 22; a loud throw when `assets.length > MAX_SUMMARY_ROWS`.
- New file: `layout-builder/src/config.test.js` — asserts `DATA_START_ROW === 23` (hard literal) and that the boundary is invariant when `assets.length` changes.
- `dcaLogSheet.test.js` new tests: data-region boundary asserted against the hard literal `23`; a registry-length-mutation test proving the band never crosses the fixed boundary.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin DATA_START_ROW to a fixed literal backed by MAX_SUMMARY_ROWS</name>
  <files>layout-builder/src/config.js, layout-builder/src/config.test.js</files>
  <read_first>
    - layout-builder/src/config.js (the file being modified and the source of truth — preserve the assets import + re-export, the SPREADSHEET_ID fail-fast block, and the DASHBOARD/DCA_LOG constants verbatim; only the DATA_START_ROW definition and its banner change)
    - .planning/phases/02-layout-builder/02-VERIFICATION.md (the gap: gaps[].missing — pin the boundary to a fixed constant independent of assets.length; add a test asserting the hard literal)
    - .planning/phases/02-layout-builder/02-CONTEXT.md (D-05 top-of-data band, D-06 never-reference-data-region, D-07 Phase 5 open-ended A{DATA_START_ROW}:A SUMIF ranges, D-08 skeleton-only)
    - assets.json (current registry — 7 assets; confirm MAX_SUMMARY_ROWS=20 leaves comfortable headroom)
    - .planning/codebase/CONVENTIONS.md (ESM, camelCase, UPPER_SNAKE_CASE config constants, 2-space/double-quote/semicolon, inline // banner style)
  </read_first>
  <behavior>
    - config.js exports `MAX_SUMMARY_ROWS` as the integer 20.
    - config.js exports `DATA_START_ROW` as the integer 23, and that value is produced WITHOUT any reference to `assets.length` (it is derived only from the fixed MAX_SUMMARY_ROWS reservation and the fixed summary-header / transaction-header row offsets).
    - Importing config.js with the current 7-asset registry, with a hypothetical 1-asset registry, and with a hypothetical 20-asset registry all yield the same `DATA_START_ROW` (23) — the boundary does not move with the registry.
    - config.test.js: `DATA_START_ROW` strictly equals the hard literal 23; `DATA_START_ROW` equals `MAX_SUMMARY_ROWS + 3`; `MAX_SUMMARY_ROWS` (20) is >= the current `assets.length` (7) with headroom to spare.
  </behavior>
  <action>
    In config.js: replace `export const DATA_START_ROW = assets.length + 3;` (line 44) and its preceding banner with a FIXED-boundary definition that has NO `assets.length` term. Add an exported `export const MAX_SUMMARY_ROWS = 20;` — the reserved maximum number of per-asset summary rows (UPPER_SNAKE_CASE per CONVENTIONS). Then define `DATA_START_ROW` from the fixed reservation and fixed row offsets only: summary header occupies row 1, the reserved summary block occupies rows 2..(1 + MAX_SUMMARY_ROWS) i.e. rows 2-21, the transaction header row sits at (2 + MAX_SUMMARY_ROWS) i.e. row 22, and the first transaction DATA row is at (3 + MAX_SUMMARY_ROWS) i.e. row 23 — so `export const DATA_START_ROW = MAX_SUMMARY_ROWS + 3;` evaluates to the literal 23 and never references `assets.length`. Rewrite the inline `//` banner above these constants to document: (1) the fixed row map (summary header row 1, reserved block rows 2-21, tx header row 22, data row 23+); (2) that this boundary is FIXED at build time and must NEVER move when the registry grows/shrinks — that floating it is the LAYOUT-02 irreversible-data-loss defect this change closes (cite the gap); (3) that `--update` must never write to or clear any row at or below `DATA_START_ROW` (D-06); (4) that Phase 5 open-ended `A{DATA_START_ROW}:A` SUMIF ranges (D-07) are compatible with and improved by a fixed, generous boundary. Justify MAX_SUMMARY_ROWS=20 in the banner: covers the current 7 assets plus comfortable growth headroom. Keep `import assets ...` and `export { assets }`, the SPREADSHEET_ID fail-fast block, and `DASHBOARD`/`DCA_LOG` verbatim. 2-space indent, double quotes, semicolons, inline // banners matching the existing style.

    Create config.test.js (bun:test, co-located) importing `./testEnv.js` FIRST (so SPREADSHEET_ID exists before config.js evaluates — same idiom as dcaLogSheet.test.js line 9), then importing `assets`, `DATA_START_ROW`, and `MAX_SUMMARY_ROWS` from `./config.js`. Add tests asserting: `DATA_START_ROW` is the exact integer 23 (a hard-coded literal in the test, NOT recomputed from assets.length); `DATA_START_ROW === MAX_SUMMARY_ROWS + 3`; `MAX_SUMMARY_ROWS === 20`; and `MAX_SUMMARY_ROWS >= assets.length` with a comment that exceeding it must be a guarded failure (covered in Task 2). Add a comment explaining that asserting the LITERAL (not a registry-derived value) is the whole point — it makes any future registry change that would move the boundary fail this suite loudly.
  </action>
  <verify>
    <automated>cd layout-builder && bun test src/config.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `config.js` contains `export const MAX_SUMMARY_ROWS = 20;` and a `DATA_START_ROW` definition with NO `assets.length` substring in its expression: `grep -n "DATA_START_ROW" layout-builder/src/config.js` shows the export, and `grep -E "DATA_START_ROW\s*=.*assets\.length" layout-builder/src/config.js` returns NOTHING.
    - `bun test src/config.test.js` passes.
    - config.test.js asserts `DATA_START_ROW` equals the integer literal `23` (the literal appears in the test source) and asserts `DATA_START_ROW === MAX_SUMMARY_ROWS + 3`.
    - With SPREADSHEET_ID set, importing config.js still exports string `DASHBOARD`/`DCA_LOG`, array `assets`, and the SPREADSHEET_ID fail-fast still throws when unset (no regression): `grep -q "process.env.SPREADSHEET_ID" layout-builder/src/config.js` succeeds.
  </acceptance_criteria>
  <done>config.js defines DATA_START_ROW as the fixed literal 23 (= MAX_SUMMARY_ROWS + 3) with zero reference to assets.length; config.test.js proves the literal and that the boundary cannot move with the registry.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reposition the DCA Log band from the fixed boundary, reserve blank rows, guard overflow, and harden the data-safety test</name>
  <files>layout-builder/src/dcaLogSheet.js, layout-builder/src/dcaLogSheet.test.js</files>
  <read_first>
    - layout-builder/src/dcaLogSheet.js (the file being modified — currently positions the whole band from the floating value: SUMMARY_HEADER_ROW=1, FIRST_SUMMARY_ROW=2, TX_HEADER_ROW = DATA_START_ROW - 1 at line 36, summary loop lines 97-106)
    - layout-builder/src/config.js (source of truth, AFTER Task 1 — now exports the fixed DATA_START_ROW=23 and MAX_SUMMARY_ROWS=20)
    - layout-builder/src/dcaLogSheet.test.js (the blind test — line 12 imports DATA_START_ROW, line 30 derives DATA_START_ROW_0BASED from it, lines 56-71 the critical assertion; must be hardened to anchor on the fixed literal and add a registry-mutation invariant test)
    - .planning/phases/02-layout-builder/02-VERIFICATION.md (gaps[].missing items 2 and 3: leave reserved rows blank rather than shifting the header; add a test that a registry change does NOT move the boundary)
    - .planning/phases/02-layout-builder/02-CONTEXT.md (D-05 band, D-06 never-address-data-region, D-08 skeleton-only — reserved blank rows stay label/format-only)
    - assets.json (7 assets — summary labels fill rows 2-8; reserved rows 9-21 stay blank; tx header row 22; data row 23)
  </read_first>
  <behavior>
    - dcaLogBuildRequests / dcaLogUpdateRequests position the transaction header at the FIXED row 22 (= DATA_START_ROW - 1) regardless of assets.length, and the summary header at row 1.
    - Per-asset summary label rows fill rows 2..(1 + assets.length) top-down (rows 2-8 for the current 7 assets). Reserved rows (2 + assets.length)..(1 + MAX_SUMMARY_ROWS) (rows 9-21) receive NO label/value request — they stay blank (D-08; no formulas).
    - When `assets.length > MAX_SUMMARY_ROWS`, both builders throw a clear Error naming MAX_SUMMARY_ROWS and the current asset count (fail loudly rather than silently shifting the boundary).
    - No request in either builder addresses any row at index >= DATA_START_ROW (0-based: >= DATA_START_ROW - 1). This holds for the current registry AND would hold for any registry up to MAX_SUMMARY_ROWS, because the boundary is fixed.
    - No request contains `formulaValue` or `addConditionalFormatRule` (D-08); `dcaLogUpdateRequests` is deterministic across two calls.
  </behavior>
  <action>
    In dcaLogSheet.js: import `MAX_SUMMARY_ROWS` in addition to the existing `assets, DCA_LOG, DATA_START_ROW` from `./config.js`. Keep the band positioned from the FIXED boundary — `TX_HEADER_ROW` stays `DATA_START_ROW - 1` (now resolves to the fixed 22), and `SUMMARY_HEADER_ROW`/`FIRST_SUMMARY_ROW` stay 1 and 2. In `bandRequests`, change the per-asset summary loop so it emits one label row per asset filling rows `FIRST_SUMMARY_ROW + i` for `i` in `0..assets.length-1` (top-down within the reserved block) and emits NO request for the reserved-but-unused rows `FIRST_SUMMARY_ROW + assets.length .. (1 + MAX_SUMMARY_ROWS)` — those stay blank. Update the number-format requests for the summary columns so their range covers the reserved block consistently with the fixed layout (format the reserved summary rows up to row `1 + MAX_SUMMARY_ROWS`, i.e. endRowIndex bounded at `MAX_SUMMARY_ROWS + 1` 0-based-exclusive, which is strictly above DATA_START_ROW - 1) — formats are allowed in the reserved/blank rows since they carry no value and are still above the data region (D-08 label/format-only). At the top of `bandRequests`, add a loud guard: if `assets.length > MAX_SUMMARY_ROWS`, throw an Error like "assets.length (N) exceeds MAX_SUMMARY_ROWS (20); increase MAX_SUMMARY_ROWS in config.js and re-run --build — refusing to silently shift DATA_START_ROW". Update the banner comments (lines 9-13, 84-86) to describe the FIXED row map (header row 1, summary rows fill 2..1+N, reserved blank rows up to 21, tx header row 22, data row 23+) and to state that the boundary is fixed and never derived from assets.length — closing the LAYOUT-02 floating-boundary defect. Do NOT introduce read-detect-write logic. Keep skeleton-only (no formulaValue, no addConditionalFormatRule, no dropdowns).

    In dcaLogSheet.test.js: harden the data-region assertion so it is anchored to the FIXED literal, not a value re-derived from the registry. Add a hard-coded `const DATA_START_ROW_LITERAL = 23;` and assert `DATA_START_ROW === DATA_START_ROW_LITERAL` (importing the constant only to prove it equals the literal — the assertion boundary must be the literal). Change `DATA_START_ROW_0BASED` (line 30) to derive from `DATA_START_ROW_LITERAL` (i.e. `23 - 1 = 22`) so the critical assertion (lines 56-71) checks ranges against the fixed boundary, not a floating one. Add a NEW test proving the invariant under registry change: temporarily mutate the imported `assets` array length (e.g. push a fake asset, or run the builders with a stubbed registry) — at minimum assert that the transaction header row found in `dcaLogBuildRequests` is at 0-based row index `DATA_START_ROW_LITERAL - 2` (row 22 1-based, the header) and that adding an asset does NOT change which row the header lands on; if directly mutating the shared `assets` import is impractical, instead assert that the header row index and the max range `endRowIndex` across all requests are both independent of `assets.length` by checking they equal their fixed literals (header at 0-based 21, no range endRowIndex exceeds 22). Add a test asserting the overflow guard throws when given a registry larger than MAX_SUMMARY_ROWS (if the builders read the shared import, document the chosen approach — e.g. a dedicated guard helper or asserting the throw via a length check). Keep the existing skeleton-only and idempotency tests passing.
  </action>
  <verify>
    <automated>cd layout-builder && bun test src/dcaLogSheet.test.js && bun test</automated>
  </verify>
  <acceptance_criteria>
    - `bun test src/dcaLogSheet.test.js` passes and the full `bun test` suite passes (no regression in dashboard/config/index tests).
    - dcaLogSheet.test.js contains a hard-coded `23` (DATA_START_ROW literal) and its data-region assertion is bounded against that literal — NOT against a value recomputed from `assets.length`: `grep -n "23" layout-builder/src/dcaLogSheet.test.js` shows the literal used as the boundary.
    - A test proves the transaction header row index does not move when the asset count changes (boundary invariant), and a test proves `assets.length > MAX_SUMMARY_ROWS` triggers a loud Error.
    - dcaLogSheet.js: the per-asset summary loop emits exactly `assets.length` label rows (rows 2..1+N) and emits NO label request for rows N+2..21; `grep -q "MAX_SUMMARY_ROWS" layout-builder/src/dcaLogSheet.js` succeeds.
    - The critical assertion still holds: NO update request range has `endRowIndex > 22` (0-based-exclusive boundary = DATA_START_ROW - 1 = 22) and none begins at/inside the data region; both builders remain formula-free and conditional-format-free; `dcaLogUpdateRequests(0)` is deep-equal across two calls.
  </acceptance_criteria>
  <done>dcaLogSheet.js positions the band from the fixed boundary, fills summary labels top-down, leaves reserved rows blank, and throws on overflow; dcaLogSheet.test.js is anchored to the hard literal 23, proves the boundary is invariant under registry change, and the full suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| asset registry (assets.json) → DCA Log band layout | A registry edit (the documented one-line CONFIG-01 add/remove) drives the band geometry; if the data boundary tracks the registry, the edit becomes a data-loss trigger |
| builder request set → Google Sheets API | request ranges define what `--update` writes/clears; any range at/below the fixed boundary = irreversible loss of hand-entered DCA transactions |
| config.js DATA_START_ROW → all band positioning + Phase 5 SUMIF ranges | a single boundary constant is the contract every DCA Log range and future formula is bounded against |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Tampering / data loss | Floating DATA_START_ROW re-stamps the transaction header onto a live DCA data row after a registry edit (the gap this plan closes) | mitigate | Task 1 pins DATA_START_ROW to the fixed literal 23 (= MAX_SUMMARY_ROWS + 3), with NO assets.length term; Task 2 positions the whole band from that fixed boundary so a registry edit never moves the header — directly eliminating the LAYOUT-02 irreversible-data-loss class |
| T-02-10 | Tampering / data loss | Test suite is blind to the floating-boundary overwrite (assertion derived from the same moving value, stays green while rows are clobbered) | mitigate | Task 1 + Task 2 anchor the data-safety assertions to a HARD literal (23) and add a registry-mutation invariant test, so any future change that would move the boundary fails the suite loudly instead of passing silently |
| T-02-11 | Tampering / data loss | Registry grows beyond the reserved summary block and silently shifts the boundary into the data region | mitigate | Task 2 adds a loud guard: builders throw when assets.length > MAX_SUMMARY_ROWS, refusing to shift DATA_START_ROW silently (fail-closed) |
| T-02-12 | Tampering | Reserved blank summary rows accidentally carry formulas/conditional formatting (scope creep past D-08) | mitigate | Reserved rows stay label/format-only; skeleton-only assertions (no formulaValue / no addConditionalFormatRule) retained and run over both builders |
| T-02-SC | Tampering | npm/package installs | accept | No new package-manager installs this plan — only edits to existing layout-builder source/tests; `googleapis` already declared (Phase 1); no install task present |
</threat_model>

<verification>
- `cd layout-builder && bun test` passes the full suite (config, dashboard, dcaLog, index) with the new boundary tests green.
- `grep -E "DATA_START_ROW\s*=.*assets\.length" layout-builder/src/config.js` returns nothing (boundary no longer derived from the registry).
- `grep -q "MAX_SUMMARY_ROWS" layout-builder/src/config.js layout-builder/src/dcaLogSheet.js` succeeds (reservation wired through).
- dcaLogSheet.test.js asserts the data-region boundary against the hard literal `23` and includes a registry-invariant test plus an overflow-guard test.
- `grep -rn "formulaValue\|addConditionalFormatRule" layout-builder/src/dcaLogSheet.js` matches only comments (D-08 skeleton-only preserved).
- `grep -rn "apps-script" layout-builder/src/` returns nothing (two-runtime isolation preserved).
</verification>

<success_criteria>
- DATA_START_ROW is a fixed integer literal (23 = MAX_SUMMARY_ROWS + 3) decided at build time, with zero reference to assets.length (LAYOUT-02 root defect removed).
- After a CONFIG-01 asset add/remove, `--update` re-stamps the transaction header at the same fixed row 22 and never addresses any previously-existing DCA data row (LAYOUT-02 unconditional data safety).
- Per-asset summary rows fill top-down up to assets.length; reserved rows stay blank (D-08); assets.length > MAX_SUMMARY_ROWS fails loudly.
- The data-safety test is anchored to the hard literal and includes a registry-invariant assertion, so a future boundary-moving change fails the suite loudly; full `bun test` suite green.
</success_criteria>

<output>
Create `.planning/phases/02-layout-builder/02-03-SUMMARY.md` when done.
</output>
