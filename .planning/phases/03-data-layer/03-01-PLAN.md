---
phase: 03-data-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - assets.json
  - apps-script/appsscript.json
  - apps-script/src/Properties.ts
autonomous: true
requirements: [SEC-01, SEC-02]
must_haves:
  truths:
    - "assets.json carries the four verified Solana mints and the three HL spot tickers (UBTC/HYPE/XAUT0) — no PLACEHOLDER_* strings remain"
    - "appsscript.json grants exactly one OAuth scope: script.external_request (no cloud-platform, no spreadsheets this phase)"
    - "A getScriptProp(name) reader returns the Script Property value and throws a descriptive error when it is missing/empty (never returns null/empty silently)"
    - "No real wallet address or Jupiter key literal is committed in source (setup() ships with placeholders only)"
  artifacts:
    - path: "assets.json"
      provides: "Real mints + HL spot tickers (D-04, D-05)"
      contains: "XAUT0"
    - path: "apps-script/appsscript.json"
      provides: "external_request OAuth scope (DATA-01/02/03 prereq)"
      contains: "script.external_request"
    - path: "apps-script/src/Properties.ts"
      provides: "getScriptProp fail-loud reader + placeholder setup() helper (SEC-01/SEC-02)"
      exports: ["getScriptProp", "setup"]
      min_lines: 25
  key_links:
    - from: "apps-script/src/Properties.ts"
      to: "PropertiesService.getScriptProperties()"
      via: "getProperty(name) read with null guard"
      pattern: "PropertiesService\\.getScriptProperties\\(\\)\\.getProperty"
---

<objective>
Lay the config + secrets foundation both providers depend on, with zero network code:
1. Replace the `assets.json` placeholders with the verified Solana mints (D-04) and the real Hyperliquid **spot** tickers `UBTC`/`HYPE`/`XAUT0` (D-05).
2. Add the single `script.external_request` OAuth scope to `appsscript.json` — the hard blocking prerequisite that lets `UrlFetchApp` make any outbound call.
3. Create `Properties.ts` with a fail-loud `getScriptProp()` reader (SEC-01/SEC-02) and a one-time `setup()` helper that ships with PLACEHOLDER literals only (never real secrets — SEC-02).

Purpose: Both providers (Plan 02) read `ASSETS` real values + `getScriptProp()`; without the scope they cannot fetch at all. Isolating this as pure config keeps the provider plan focused on parsing logic.
Output: Edited `assets.json`, edited `appsscript.json`, new `apps-script/src/Properties.ts`.
</objective>

<execution_context>
@/home/bagashiz/Projects/crypto-portfolio-tracker/.claude/gsd-core/workflows/execute-plan.md
@/home/bagashiz/Projects/crypto-portfolio-tracker/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-data-layer/03-CONTEXT.md
@.planning/phases/03-data-layer/03-RESEARCH.md
@.planning/phases/03-data-layer/03-PATTERNS.md
@apps-script/src/Config.ts
</context>

<artifacts_this_phase_produces>
This plan produces (full phase inventory spans Plans 01-03):
- **assets.json** — real values: `XAUt` ticker `XAUT0`, `BTC` ticker `UBTC`, mints `CqW2pd6dCPG9xKZfAsTovzDsMmAGKJSDBNcwM96ondo` (IVVon), `59obFNBzyTBGowrkif5uK7ojS58vsuWz3ZCvg6tfZAGw` (PST), `5Y8NV33Vv7WbnLfq3zBcKSdYPrk7g2KoiQoe7M2tcxp5` (ONyc), `A1KLoBrKBde8Ty9qtNQUtq3C2ortoC3u7twggz7sEto6` (USDy).
- **appsscript.json** — new OAuth scope `https://www.googleapis.com/auth/script.external_request`.
- **apps-script/src/Properties.ts** — `export function getScriptProp(name: string): string`, `export function setup(): void`.
- **New Script Property keys** (consumed at runtime, NOT committed): `HL_WALLET_ADDRESS`, `SOL_WALLET_ADDRESS`, `JUP_API_KEY`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Fill assets.json with verified mints and HL spot tickers (D-04, D-05)</name>
  <files>assets.json</files>
  <read_first>
    - assets.json (current placeholders at lines 5, 21, 29, 37, 45, 53)
    - 03-PATTERNS.md lines 126-134 (assets.json fill pattern, exact value mapping)
    - 03-CONTEXT.md D-04/D-05 (lines 36-45 — authoritative values + rationale)
    - apps-script/src/Config.ts lines 13-28 (Asset interface — shape MUST stay unchanged)
  </read_first>
  <action>
    Replace the five placeholder literals in `assets.json`, leaving every other field (id, venue, target, risk, apy) and the file shape unchanged. BTC: change `ticker` from `"BTC"` to `"UBTC"` (D-05 — the wallet holds the bridged Unit BTC spot token; perp `BTC` is a different instrument). XAUt: change `ticker` from `"PLACEHOLDER_TICKER_phase3"` to `"XAUT0"` (token index 297). HYPE ticker stays `"HYPE"`. Replace the four `"PLACEHOLDER_MINT_phase3"` mints in id order: IVVon → `CqW2pd6dCPG9xKZfAsTovzDsMmAGKJSDBNcwM96ondo`, PST → `59obFNBzyTBGowrkif5uK7ojS58vsuWz3ZCvg6tfZAGw`, ONyc → `5Y8NV33Vv7WbnLfq3zBcKSdYPrk7g2KoiQoe7M2tcxp5`, USDy → `A1KLoBrKBde8Ty9qtNQUtq3C2ortoC3u7twggz7sEto6`. Keep 2-space indent, double quotes, trailing newline (CLAUDE.md style).
  </action>
  <verify>
    <automated>cd /home/bagashiz/Projects/crypto-portfolio-tracker && bun -e "const a=require('./assets.json'); if(JSON.stringify(a).includes('PLACEHOLDER')) throw new Error('placeholder remains'); const byId=Object.fromEntries(a.map(x=>[x.id,x])); if(byId.BTC.ticker!=='UBTC') throw new Error('BTC ticker'); if(byId.XAUt.ticker!=='XAUT0') throw new Error('XAUt ticker'); if(byId.HYPE.ticker!=='HYPE') throw new Error('HYPE ticker'); if(byId.IVVon.mint!=='CqW2pd6dCPG9xKZfAsTovzDsMmAGKJSDBNcwM96ondo') throw new Error('IVVon mint'); if(byId.USDy.mint!=='A1KLoBrKBde8Ty9qtNQUtq3C2ortoC3u7twggz7sEto6') throw new Error('USDy mint'); console.log('assets.json OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `bun -e` assertion above prints "assets.json OK" and exits 0.
    - `grep -c PLACEHOLDER assets.json` returns 0.
    - BTC.ticker === "UBTC", XAUt.ticker === "XAUT0", HYPE.ticker === "HYPE".
    - All four Solana mints match the D-04 values exactly (IVVon/PST/ONyc/USDy).
    - The 7-element array shape and all non-placeholder fields are byte-identical to before.
  </acceptance_criteria>
  <done>assets.json contains the verified mints and HL spot tickers; no PLACEHOLDER strings; Asset shape unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add external_request OAuth scope to appsscript.json</name>
  <files>apps-script/appsscript.json</files>
  <read_first>
    - apps-script/appsscript.json (current `"oauthScopes": []` at line 5)
    - 03-PATTERNS.md lines 112-122 (OAuth scope fill pattern — minimal-scope rationale)
    - 03-RESEARCH.md lines 439, 444-446 (external_request is the single hard blocking prerequisite)
    - 03-CONTEXT.md D-77/Discretion lines 77 (only external_request this phase; spreadsheets/script.scriptapp deferred to Phase 4)
  </read_first>
  <action>
    Change `oauthScopes` from `[]` to an array containing exactly one entry: `"https://www.googleapis.com/auth/script.external_request"`. Do NOT add `cloud-platform` (Secret Manager is removed per D-07), `spreadsheets`, or `script.scriptapp` — those land in Phase 4 (minimal-scope mitigation for the over-broad-OAuth-scope threat). Preserve the other keys (`timeZone`, `runtimeVersion`, `exceptionLogging`) unchanged; keep 2-space indent.
  </action>
  <verify>
    <automated>cd /home/bagashiz/Projects/crypto-portfolio-tracker && bun -e "const m=require('./apps-script/appsscript.json'); const s=m.oauthScopes||[]; if(s.length!==1) throw new Error('expected exactly 1 scope, got '+s.length); if(s[0]!=='https://www.googleapis.com/auth/script.external_request') throw new Error('wrong scope: '+s[0]); if(JSON.stringify(m).includes('cloud-platform')||JSON.stringify(m).includes('spreadsheets')||JSON.stringify(m).includes('scriptapp')) throw new Error('over-broad scope present'); console.log('oauthScopes OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `oauthScopes` is an array of length exactly 1.
    - The single entry equals `https://www.googleapis.com/auth/script.external_request`.
    - No `cloud-platform`, `spreadsheets`, or `script.scriptapp` scope is present.
    - `timeZone`, `runtimeVersion`, `exceptionLogging` keys are unchanged.
  </acceptance_criteria>
  <done>appsscript.json grants only external_request; providers can make outbound UrlFetchApp calls; no over-broad scope.</done>
</task>

<task type="auto">
  <name>Task 3: Create Properties.ts — fail-loud getScriptProp() + placeholder setup() (SEC-01/SEC-02)</name>
  <files>apps-script/src/Properties.ts</files>
  <read_first>
    - apps-script/src/Config.ts lines 1-8 (module shape: JSDoc banner + named exports + import-type convention to mirror)
    - apps-script/src/Hello.ts (named export style, JSDoc banner)
    - 03-RESEARCH.md Pattern 3 lines 234-256 (getScriptProp fail-loud + setup() with placeholder literals)
    - 03-PATTERNS.md lines 65-73, 156-165 (Properties.ts role-match analog + fail-loud read pattern)
    - 03-CONTEXT.md D-11 (the three property keys), Pitfall 4 in RESEARCH lines 355-359 (null property → silent wrong behavior)
  </read_first>
  <action>
    Create `apps-script/src/Properties.ts` with a top-of-file JSDoc banner (mirror Config.ts style) and two named exports. (a) `export function getScriptProp(name: string): string` — read `PropertiesService.getScriptProperties().getProperty(name)`; if the value is null or empty string, `throw new Error("Missing Script Property: " + name)` (fail loud, never a silent default — Pitfall 4). (b) `export function setup(): void` — call `PropertiesService.getScriptProperties().setProperties({ ... })` for the three keys `HL_WALLET_ADDRESS`, `SOL_WALLET_ADDRESS`, `JUP_API_KEY`, but assign each a clearly-fake PLACEHOLDER string literal (e.g. `"PLACEHOLDER_set_me_in_editor"`). Add a JSDoc note on `setup()`: the user edits these to real values LOCALLY and runs it ONCE from the editor, then reverts — real wallet/key literals must NEVER be committed (SEC-02). Do NOT `Logger.log` any property value. Use 2-space indent, double quotes, semicolons, trailing newline; PascalCase filename, camelCase functions. No `import type` needed unless a type is referenced.
  </action>
  <verify>
    <automated>cd /home/bagashiz/Projects/crypto-portfolio-tracker/apps-script && bunx tsc --noEmit -p tsconfig.json && cd /home/bagashiz/Projects/crypto-portfolio-tracker && grep -v '^\s*//' apps-script/src/Properties.ts | grep -v '^\s*\*' | grep -Eic '0x[a-f0-9]{6,}|[1-9A-HJ-NP-Za-km-z]{32,}' | grep -qx 0 && echo "no real secret literals" || (echo "SUSPECTED real wallet/key literal in Properties.ts" && exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - `bunx tsc --noEmit` over apps-script passes with no type errors (getScriptProp returns string; strict mode satisfied).
    - `Properties.ts` exports both `getScriptProp` and `setup` as named exports.
    - `getScriptProp` throws on a null/empty property (assertion: source contains a `throw` guarding the null/empty case).
    - `setup()` references all three keys `HL_WALLET_ADDRESS`, `SOL_WALLET_ADDRESS`, `JUP_API_KEY`.
    - The secret-literal grep gate (excluding comment lines) finds NO hex `0x…` address or base58 mint/key-shaped literal — only PLACEHOLDER strings (SEC-02).
  </acceptance_criteria>
  <done>Properties.ts compiles, exports getScriptProp (fail-loud) and setup (placeholders only), commits no real secrets.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed source → git history | Secrets must never cross this boundary (SEC-02) |
| Script Property store → runtime | Config read at call time; absence must fail loud, not default |
| Apps Script → external HTTP | Outbound capability gated by OAuth scope |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | `setup()` literals / committed source | mitigate | `setup()` ships PLACEHOLDER strings only; Task 3 grep gate rejects hex-address / base58 key-shaped literals; live values set locally and reverted (SEC-02) |
| T-03-02 | Information Disclosure | `JUP_API_KEY` in logs | mitigate | `Properties.ts` never `Logger.log`s a property value (V7); key only flows to the `x-api-key` header in Plan 02 |
| T-03-03 | Elevation of Privilege | `appsscript.json` OAuth scopes | mitigate | Add only `script.external_request`; Task 2 gate rejects `cloud-platform`/`spreadsheets`/`scriptapp` (minimal scope) |
| T-03-04 | Tampering / DoS | missing Script Property → `x-api-key: null` 401 loop | mitigate | `getScriptProp()` throws on null/empty (Pitfall 4) — fails loud instead of sending a null header |
| T-03-SC | Tampering | npm/bun installs | accept | No packages installed this phase (RESEARCH Package Legitimacy Audit: zero installs); Apps Script runtime has no npm |
</threat_model>

<verification>
- `bun -e` assertions confirm assets.json + appsscript.json values (Tasks 1, 2).
- `bunx tsc --noEmit -p apps-script/tsconfig.json` confirms Properties.ts type-checks under strict mode.
- Secret-literal grep gate confirms no real wallet/key in committed source (SEC-02).
- `grep -c PLACEHOLDER assets.json` returns 0 (no leftover placeholders).
</verification>

<success_criteria>
- assets.json carries the 4 verified mints + 3 HL spot tickers (D-04/D-05); no PLACEHOLDER strings.
- appsscript.json grants exactly `script.external_request` and nothing broader.
- Properties.ts exports a fail-loud `getScriptProp` and a placeholder-only `setup`; compiles clean; commits no secrets.
- Maps to SEC-01 (key read path), SEC-02 (no committed secrets, PropertiesService config surface).
</success_criteria>

<output>
Create `.planning/phases/03-data-layer/03-01-SUMMARY.md` when done.
</output>
