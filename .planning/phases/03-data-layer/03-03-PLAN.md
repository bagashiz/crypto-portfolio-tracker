---
phase: 03-data-layer
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - apps-script/src/entry.ts
  - apps-script/scripts/appendGlobals.ts
  - apps-script/src/globals.d.ts
autonomous: false
requirements: [SEC-01, SEC-02, DATA-01, DATA-02, DATA-03]
must_haves:
  truths:
    - "Both providers are reachable from entry.ts so bun build retains them in dist/Code.js (not tree-shaken)"
    - "setup() is exposed as a top-level editor-callable global via the __ENTRY__ + appendGlobals + globals.d.ts mechanism (D-12)"
    - "bun run build succeeds and dist/Code.js contains getHyperliquidData, getJupiterData, and a top-level function setup() shim"
    - "A human confirms (in the deployed editor) that the providers return real prices/balances for the live wallets, and that no real secret was committed"
  artifacts:
    - path: "apps-script/src/entry.ts"
      provides: "Provider import/retention + setup in __ENTRY__"
      contains: "getHyperliquidData"
    - path: "apps-script/scripts/appendGlobals.ts"
      provides: "setup added to ENTRY_GLOBALS"
      contains: "setup"
    - path: "apps-script/src/globals.d.ts"
      provides: "ambient var setup decl"
      contains: "setup"
  key_links:
    - from: "apps-script/src/entry.ts"
      to: "apps-script/src/HyperliquidApi.ts + JupiterApi.ts"
      via: "import (bundle retention, Pitfall 5)"
      pattern: "import \\{ getHyperliquidData"
    - from: "apps-script/scripts/appendGlobals.ts"
      to: "dist/Code.js top-level function setup()"
      via: "ENTRY_GLOBALS shim"
      pattern: "\"setup\""
---

<objective>
Wire the providers into the build and surface the `setup()` config helper as an editor global, then verify the whole thing live.

Two parts:
1. **Bundle retention + global exposure (auto):** `bun build` tree-shakes anything unreachable from `entry.ts` (Pitfall 5). Import both providers into `entry.ts` so they survive into `dist/Code.js`. Expose `setup()` (from Plan 01's `Properties.ts`) as a statically-discoverable editor global via the established three-edit mechanism (`__ENTRY__` object + `ENTRY_GLOBALS` array + `globals.d.ts` ambient decl, D-12). Providers stay INTERNAL (not editor entry points) but must be retained.
2. **Live verification (human-verify checkpoint):** The Apps Script editor is the only place to run code against the real wallets and the live `JUP_API_KEY` (committing real literals would violate SEC-02). The user sets the three Script Properties, runs the providers from the editor, and confirms real prices/balances come back — and that no secret landed in git.

Purpose: Closes DATA-01/02/03 end-to-end (deployed + runnable) and SEC-01/SEC-02 (live config in PropertiesService, nothing committed).
Output: edited `entry.ts`, `appendGlobals.ts`, `globals.d.ts`; a live-verified deployment.
</objective>

<execution_context>
@/home/bagashiz/Projects/crypto-portfolio-tracker/.claude/gsd-core/workflows/execute-plan.md
@/home/bagashiz/Projects/crypto-portfolio-tracker/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-data-layer/03-CONTEXT.md
@.planning/phases/03-data-layer/03-PATTERNS.md
@apps-script/src/entry.ts
@apps-script/scripts/appendGlobals.ts
@apps-script/src/globals.d.ts
@apps-script/src/Properties.ts
</context>

<artifacts_this_phase_produces>
- **entry.ts** — imports `{ getHyperliquidData }` and `{ getJupiterData }` (retention) + `{ setup }`; adds `setup` to `__ENTRY__`; references providers so the bundler keeps them.
- **appendGlobals.ts** — `ENTRY_GLOBALS` becomes `["hello", "setup"]`.
- **globals.d.ts** — adds `var setup: () => void;`.
- **New editor-callable global:** `setup()` (D-12). Providers `getHyperliquidData`/`getJupiterData` are retained but remain internal (Phase 4 calls them via `refreshAll`).
- **New Script Property keys populated live (NOT committed):** `HL_WALLET_ADDRESS`, `SOL_WALLET_ADDRESS`, `JUP_API_KEY`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Wire providers into the bundle + expose setup() as an editor global (D-12, Pitfall 5)</name>
  <files>apps-script/src/entry.ts, apps-script/scripts/appendGlobals.ts, apps-script/src/globals.d.ts</files>
  <read_first>
    - apps-script/src/entry.ts lines 24-50 (existing __ENTRY__ pattern; TODO marker at 43-46 points exactly here)
    - apps-script/scripts/appendGlobals.ts line 24 (ENTRY_GLOBALS array + idempotent shim mechanism)
    - apps-script/src/globals.d.ts lines 9-19 (ambient var pattern; reserved slots)
    - 03-PATTERNS.md lines 75-108 (entry/appendGlobals/globals.d.ts extend-in-place patterns), lines 187-193 (bundle-retention)
    - 03-RESEARCH.md Pitfall 5 lines 361-365 (tree-shaking; import providers from entry.ts), lines 244-256 (setup as D-12 global)
    - 03-CONTEXT.md D-12 (providers internal; new globals follow the three-edit mechanism)
  </read_first>
  <action>
    Three coordinated edits, mirroring how `hello` is wired.
    (1) `entry.ts`: add `import { getHyperliquidData } from "./HyperliquidApi";`, `import { getJupiterData } from "./JupiterApi";`, and `import { setup } from "./Properties";`. Add `setup` to the `__ENTRY__` object: `(globalThis as any).__ENTRY__ = { hello, setup };`. Retain the providers in the bundle WITHOUT making them editor entry points (D-12 keeps them internal): reference them so the tree-shaker keeps them — e.g. `(globalThis as any).__PROVIDERS__ = { getHyperliquidData, getJupiterData };` (a non-picker namespace, distinct from `__ENTRY__`). Remove/replace the TODO marker text at lines 43-46 to reflect that providers now ship (refreshAll wiring remains a Phase 4 TODO). Do NOT add provider names to the editor-picker path.
    (2) `appendGlobals.ts`: change `const ENTRY_GLOBALS = ["hello"] as const;` to `["hello", "setup"] as const;`. Do NOT add provider names here (internal, D-12).
    (3) `globals.d.ts`: add `var setup: () => void;` alongside `var hello` (mirror line 11 style, keep the eslint-disable comment pattern).
  </action>
  <verify>
    <automated>cd /home/bagashiz/Projects/crypto-portfolio-tracker/apps-script && bunx tsc --noEmit -p tsconfig.json && bun run build && grep -q "getHyperliquidData" dist/Code.js && grep -q "getJupiterData" dist/Code.js && grep -Eq "function setup\\(\\)" dist/Code.js && grep -q "function hello\\(\\)" dist/Code.js && ! grep -q "function getHyperliquidData()" dist/Code.js && echo "BUNDLE OK: providers retained, setup+hello are top-level shims, providers are NOT editor globals"</automated>
  </verify>
  <acceptance_criteria>
    - `bunx tsc --noEmit` passes; `bun run build` succeeds.
    - `dist/Code.js` CONTAINS the strings `getHyperliquidData` and `getJupiterData` (providers retained, not tree-shaken — Pitfall 5).
    - `dist/Code.js` contains a top-level `function setup()` shim AND the existing `function hello()` shim (appendGlobals ran with `["hello","setup"]`).
    - `dist/Code.js` does NOT contain a top-level `function getHyperliquidData()` shim (providers stay internal per D-12 — they appear as bundled functions/references, not as editor-picker shims).
    - `appendGlobals.ts` ENTRY_GLOBALS === `["hello","setup"]`; `globals.d.ts` declares `var setup`.
  </acceptance_criteria>
  <done>Build succeeds; both providers are retained in dist/Code.js; setup() is a discoverable editor global; providers remain internal.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Live Script-Properties setup + provider smoke test + no-secret-committed check</name>
  <what-built>
    Two deployed providers (`getHyperliquidData`, `getJupiterData`) returning `Record<id,{price,qty}>`, and an editor-callable `setup()` helper that writes the three Script Properties. The build retains the providers in `dist/Code.js`; `external_request` scope is granted. All code is committed with PLACEHOLDER secrets only.
  </what-built>
  <how-to-verify>
    1. Deploy: from repo root run `cd apps-script && bun run deploy` (builds + `clasp push`). On first push, clasp may prompt to authorize the new `external_request` scope — approve it.
    2. Set the three Script Properties using ONE of:
       (a) Open the Apps Script editor → Project Settings → Script Properties, and add `HL_WALLET_ADDRESS`, `SOL_WALLET_ADDRESS`, `JUP_API_KEY` with your real values; OR
       (b) Edit `setup()` in `Properties.ts` LOCALLY to your real values, `bun run deploy`, run `setup` once from the editor function picker, then REVERT the file to PLACEHOLDERs and redeploy (so no real value is committed).
    3. In the editor, run `getHyperliquidData` (add a temporary `Logger.log(JSON.stringify(getHyperliquidData()))` wrapper if the picker won't run an internal fn directly, or call it from a scratch function) and confirm the execution log shows BTC/HYPE/XAUt entries with `price` finite numbers > 0 and plausible `qty` values matching your wallet.
    4. Run `getJupiterData` similarly and confirm IVVon/PST/ONyc/USDy entries with finite `price` and plausible `qty` (no 401 → key is set correctly; no throw → all four mints priced).
    5. Confirm NO secret was committed: from repo root run `rtk git grep -nE "0x[0-9a-fA-F]{6,}|[1-9A-HJ-NP-Za-km-z]{32,}" -- apps-script assets.json` and verify it returns only the four known Solana MINT addresses in assets.json (public, intended) and the UBTC/XAUT0 tickers — and NO wallet address or API key. If a real wallet/key literal appears in any committed file, STOP and scrub it before approving.
  </how-to-verify>
  <acceptance_criteria>
    - `bun run deploy` pushes successfully with the `external_request` scope authorized.
    - `getHyperliquidData()` returns real finite spot prices (>0) and wallet-matching qtys for BTC/HYPE/XAUt.
    - `getJupiterData()` returns real finite prices and wallet-matching qtys for IVVon/PST/ONyc/USDy with no 401 (key sourced from `JUP_API_KEY`).
    - `git grep` confirms no committed wallet address or API key (only the public Solana mints in assets.json, which are intended).
  </acceptance_criteria>
  <resume-signal>Type "approved" once both providers return real live data and no secret is committed, or describe what failed (wrong price/instrument, 401, missing asset, or a committed secret).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local source → committed git | Real wallet/key must never cross (SEC-02) |
| editor `setup()` → Script Property store | Live secret values written here, not to source |
| clasp push → deployed scopes | Scope grant must stay minimal (external_request only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10 | Information Disclosure | real secret committed via edited `setup()` | mitigate | Checkpoint Task 2 step 5 `git grep` gate for hex-address / base58 key literals; revert-after-run workflow; blocking-human gate cannot auto-approve |
| T-03-11 | Elevation of Privilege | clasp authorizes more than external_request | mitigate | `appsscript.json` (Plan 01) grants only external_request; deploy prompt reviewed at approval |
| T-03-12 | Tampering (integrity) | provider returns wrong/empty live data unnoticed | mitigate | Human confirms real finite prices (>0) + wallet-matching qtys for all tracked ids before approving |
| T-03-SC | Tampering | clasp push of bundled dist/ | accept | No new packages; `clasp` vetted in Phase 1; `dist/Code.js` is reproducibly rebuilt, never hand-edited |
</threat_model>

<verification>
- `bunx tsc --noEmit` + `bun run build` confirm the wiring type-checks and bundles.
- dist/Code.js grep confirms provider retention + setup/hello top-level shims + providers NOT exposed as editor globals.
- Human-verify checkpoint confirms live data correctness and zero committed secrets — the parts no automated check in this repo can reach (live wallets, deployed editor, secret hygiene).
</verification>

<success_criteria>
- Providers retained in dist/Code.js; `setup()` editor-callable (D-12); build green.
- Deployed providers return real live prices/balances for all tracked ids (DATA-01/02/03 proven end-to-end).
- Script Properties hold the live config; no wallet/key committed (SEC-01/SEC-02).
</success_criteria>

<output>
Create `.planning/phases/03-data-layer/03-03-SUMMARY.md` when done.
</output>
