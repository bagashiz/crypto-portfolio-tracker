---
phase: 05-pnl-allocation
plan: 03
type: execute
wave: 2
depends_on: ["05-02"]
files_modified:
  - apps-script/src/Refresh.ts
  - apps-script/src/Refresh.test.ts
autonomous: true
requirements: [PNL-03]
must_haves:
  truths:
    - "refreshAll() writes LastUpdated/Stale? into the relocated status columns matching the layout builder's new STATUS_START_COL (col K) — LastUpdated col L (12), Stale? col M (13)"
    - "refreshAll() writes ONLY Qty (col B) and Price (col C); it never writes Value (col D), which is now a =B*C formula"
    - "The single batched setValues writes for Qty/Price and for the status block are preserved (no cell-by-cell writes)"
  artifacts:
    - path: "apps-script/src/Refresh.ts"
      provides: "Updated STATUS_LASTUPDATED_COL constant (10 -> 12) and a confirmed 2-col Qty/Price write that excludes Value"
      contains: "STATUS_LASTUPDATED_COL"
    - path: "apps-script/src/Refresh.test.ts"
      provides: "Assertion that the Qty/Price write stays 2 cols (B:C, never D) and status geometry constants match the layout builder"
  key_links:
    - from: "apps-script/src/Refresh.ts STATUS_LASTUPDATED_COL"
      to: "layout-builder/src/dashboardSheet.js STATUS_START_COL"
      via: "shared Dashboard geometry (the only cross-runtime integration surface) — Refresh col = STATUS_START_COL + 1"
      pattern: "STATUS_LASTUPDATED_COL"
---

<objective>
Move the Apps Script refresh layer in lockstep with the Dashboard geometry change from Plan 02: the per-venue status block relocated to col K (STATUS_START_COL=11), so `refreshAll()` must write LastUpdated/Stale? at the new columns (L/M = 12/13), and it must NOT write Value (col D) because Value is now a `=B*C` formula (D-01, D-02). This is the cross-runtime coupling point — the Google Sheet is the only integration surface, and these two constants encode the same geometry in two runtimes.

Purpose: Without this, refreshAll() would write LastUpdated/Stale? into Zone A's new PnL columns (col J/K overlap) and would risk overwriting the Value formula — corrupting the live view this phase delivers.
Output: `Refresh.ts` STATUS_LASTUPDATED_COL bumped 10 → 12, Value-col-D exclusion re-confirmed; `Refresh.test.ts` asserts both.
</objective>

<execution_context>
@/home/bagashiz/Projects/crypto-portfolio-tracker/.claude/gsd-core/workflows/execute-plan.md
@/home/bagashiz/Projects/crypto-portfolio-tracker/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-pnl-allocation/05-CONTEXT.md
@.planning/phases/05-pnl-allocation/05-PATTERNS.md
@.planning/phases/05-pnl-allocation/05-02-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Move status-block write columns and re-confirm Qty/Price-only write in Refresh.ts</name>
  <files>apps-script/src/Refresh.ts</files>
  <read_first>
    - apps-script/src/Refresh.ts (the file being modified — Dashboard geometry block lines 130-147: QTY_COL, VALUE_COLS, STATUS_LASTUPDATED_COL, STATUS_HL_ROW, STATUS_SOL_ROW; the setValues calls at lines 190-214)
    - layout-builder/src/dashboardSheet.js (after Plan 02 — the new STATUS_START_COL=11 value this MUST match)
    - .planning/phases/05-pnl-allocation/05-02-PLAN.md (the chosen STATUS_START_COL and the formula: Refresh col = STATUS_START_COL + 1)
    - .planning/phases/05-pnl-allocation/05-CONTEXT.md (D-01 status relocation, D-02 Value is a formula — refresh must not write col D)
    - .planning/phases/05-pnl-allocation/05-PATTERNS.md (Refresh.ts section + Cross-runtime status-block coupling)
  </read_first>
  <behavior>
    - STATUS_LASTUPDATED_COL = STATUS_START_COL + 1 = 12 (col L); Stale? lands at col M (13) via the existing 2-col status setValues width.
    - The Zone A Qty/Price write stays exactly 2 columns wide (Qty col B + Price col C); Value (col D) is never included.
    - Both setValues calls remain single batched writes (no cell-by-cell writes introduced).
  </behavior>
  <action>
    Read the final `STATUS_START_COL` value chosen in Plan 02 (col K = 11 per 05-02-PLAN). Change `STATUS_LASTUPDATED_COL` from 10 (J) to `STATUS_START_COL + 1` = 12 (L) so LastUpdated=col L (12) and Stale?=col M (13) given the existing 2-col status `setValues` width at lines 212-214. Update the geometry comment block (lines 141-147) to reflect the new K/L/M columns (header K1:M1, Hyperliquid K2:M2, Solana K3:M3). Confirm `VALUE_COLS = 2` and the `valueRange = sheet.getRange(ZONE_A_FIRST_ASSET_ROW, QTY_COL, ASSETS.length, VALUE_COLS)` write spans ONLY Qty(B)+Price(C) — it already does; add an inline comment stating Value(D) is intentionally excluded (D-02: Value is a `=B*C` formula). Do NOT widen VALUE_COLS to include D. Do NOT change the assembler or cache logic. Keep both writes as single batched setValues.
  </action>
  <verify>
    <automated>bun test apps-script/src/Refresh.test.ts 2>&1 | head -40</automated>
  </verify>
  <acceptance_criteria>
    - `STATUS_LASTUPDATED_COL` equals 12 in Refresh.ts.
    - `VALUE_COLS` is still 2; the Qty/Price `getRange` width is 2 (B:C), never 3.
    - The geometry comment references cols K/L/M for the status block.
    - No new cell-by-cell `setValue` (singular) calls are introduced — both writes remain `setValues`.
  </acceptance_criteria>
  <done>refreshAll() writes LastUpdated/Stale? at cols L/M (matching the layout builder's relocated status block) and writes only Qty/Price (never Value col D), with both writes still single batched setValues.</done>
</task>

<task type="auto">
  <name>Task 2: Assert the cross-runtime geometry + Value-exclusion in Refresh.test.ts</name>
  <files>apps-script/src/Refresh.test.ts</files>
  <read_first>
    - apps-script/src/Refresh.test.ts (the file being modified — existing assembleRefreshRows / backfill tests and their import surface)
    - apps-script/src/Refresh.ts (after Task 1 — exported symbols; note STATUS_LASTUPDATED_COL/QTY_COL/VALUE_COLS are module-private constants, not exported)
    - .planning/phases/05-pnl-allocation/05-PATTERNS.md (Refresh.test.ts is an existing offline suite; follow its pure-assertion pattern)
  </read_first>
  <action>
    Add tests guarding the cross-runtime geometry and Value exclusion. Since the geometry constants are module-private, assert behaviorally where possible and structurally otherwise: (a) confirm `assembleRefreshRows` returns exactly 2-wide rows (`[qty, price]`) per asset for the current registry — proving the write block is Qty/Price only (2 cols), never Value; assert `row.length === 2` for every row. (b) Add a guard test that documents the expected status column: if `STATUS_LASTUPDATED_COL` is not exported, EITHER export it from Refresh.ts (preferred — add `export` to the const) and assert it equals 12, OR add a source-level assertion via reading the constant through an exported accessor. Prefer exporting `STATUS_LASTUPDATED_COL`, `QTY_COL`, and `VALUE_COLS` and asserting `VALUE_COLS === 2` and `STATUS_LASTUPDATED_COL === 12`. Keep the existing assembler/backfill tests passing unchanged.
  </action>
  <verify>
    <automated>bun test apps-script/</automated>
  </verify>
  <acceptance_criteria>
    - A test asserts every `assembleRefreshRows` output row has length 2 (Qty/Price only, no Value).
    - A test asserts `STATUS_LASTUPDATED_COL === 12` and `VALUE_COLS === 2` (constants exported from Refresh.ts for this purpose).
    - Existing assembler/backfill tests still pass.
    - `bun test apps-script/` exits 0.
  </acceptance_criteria>
  <done>The Refresh test suite proves refreshAll() writes a 2-col Qty/Price block (no Value) and that the status column matches the layout builder's relocated geometry (col L = 12).</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 03)
New symbols / changes introduced by this plan:
- Changed constant: `Refresh.ts STATUS_LASTUPDATED_COL` 10 (J) → 12 (L) — matches Plan 02's `STATUS_START_COL=11` (+1).
- Possibly newly-exported constants from `Refresh.ts`: `STATUS_LASTUPDATED_COL`, `QTY_COL`, `VALUE_COLS` (exported only to make the cross-runtime geometry test-assertable).
- Confirmed invariant (with inline comment + test): the Zone A write is 2 cols (Qty B / Price C), Value (D) excluded — D-02.
- New `Refresh.test.ts` assertions: 2-col write width + status-column geometry.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| refreshAll() write → Dashboard cells | Apps Script (read-only providers) writes Qty/Price + status into the bound Dashboard; the only mutation surface |
| cross-runtime geometry | Refresh.ts STATUS_LASTUPDATED_COL must match layout-builder STATUS_START_COL via the shared sheet (no shared code) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Tampering | refreshAll() overwriting the Value(D) formula | mitigate | VALUE_COLS pinned to 2 and asserted in Refresh.test.ts (every assembled row length 2); the getRange write spans only B:C so the =B*C formula in D is never clobbered (D-02). |
| T-05-07 | Tampering | status write landing in Zone A PnL columns | mitigate | STATUS_LASTUPDATED_COL moved to 12 (col L) to match the relocated status block; asserted === 12 in Refresh.test.ts so it cannot silently drift back onto the new PnL columns (H/I). |
| T-05-SC | Tampering | npm/bun installs | accept | This plan adds NO new dependencies — edits existing Apps Script source + its test only. No install step. |
</threat_model>

<verification>
- `bun test apps-script/` exits 0.
- STATUS_LASTUPDATED_COL === 12 and VALUE_COLS === 2 are asserted.
- The Qty/Price write is provably 2 cols wide (no Value column).
</verification>

<success_criteria>
- refreshAll() writes LastUpdated/Stale? at the relocated status columns (L/M) consistent with the layout builder's STATUS_START_COL=11 (PNL-03 cross-runtime wiring).
- refreshAll() writes only Qty/Price (never Value col D), so the =B*C Value formula and downstream PnL math stay intact.
- Both writes remain single batched setValues (REFRESH-02 preserved).
</success_criteria>

<output>
Create `.planning/phases/05-pnl-allocation/05-03-SUMMARY.md` when done.
</output>
