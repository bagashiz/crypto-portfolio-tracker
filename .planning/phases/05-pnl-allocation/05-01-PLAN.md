---
phase: 05-pnl-allocation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - layout-builder/src/dcaLogSheet.js
  - layout-builder/src/dcaLogSheet.test.js
autonomous: true
requirements: [PNL-01, PNL-02]
must_haves:
  truths:
    - "The DCA Log summary block computes Total Invested, Total Qty, DCA-weighted Avg Cost, Buy Count, Last Buy, and Total Fees per asset from BUY rows only"
    - "Summary formulas read the transaction data region via open-ended A{DATA_START_ROW}:A ranges but no summary request writes at or below row 23"
    - "An asset with no BUY rows shows the em-dash placeholder, not #DIV/0!"
    - "Avg cost is computed only here (single source of truth); the Dashboard references this cell"
  artifacts:
    - path: "layout-builder/src/dcaLogSheet.js"
      provides: "BUY-only SUMIFS/COUNTIFS/MAXIFS summary formulas via a formulaCell/formulaRowRequest helper"
      contains: "formulaValue"
    - path: "layout-builder/src/dcaLogSheet.test.js"
      provides: "Inverted skeleton assertion (summary now expects formulas) + data-region safety still enforced"
      contains: "formulaValue"
  key_links:
    - from: "layout-builder/src/dcaLogSheet.js summary Avg Cost cell"
      to: "DCA Log transaction data region (rows 23+)"
      via: "open-ended A{DATA_START_ROW}:A SUMIFS/COUNTIFS/MAXIFS formula strings (read-only)"
      pattern: "SUMIFS|COUNTIFS|MAXIFS"
---

<objective>
Add the BUY-only cost-basis summary formulas to the DCA Log top band so it becomes the single source of truth for Total Invested, Total Qty, DCA-weighted Avg Cost, Buy Count, Last Buy, and Total Fees per asset (PNL-01 enables transaction entry — the columns already exist from Phase 2; PNL-02 computes the summary). SELL rows are ignored (BUY-only, D-04 — SELL/realized PnL is Phase 6).

Purpose: The Dashboard's AvgCost column (Plan 02) references these summary cells; this is the only place cost basis is computed (no duplicated SUMIF on the Dashboard, D-03).
Output: `dcaLogSheet.js` emits summary formula cells; `dcaLogSheet.test.js` inverts the old skeleton-only assertion to expect formulas while still proving the data-region safety boundary.
</objective>

<execution_context>
@/home/bagashiz/Projects/crypto-portfolio-tracker/.claude/gsd-core/workflows/execute-plan.md
@/home/bagashiz/Projects/crypto-portfolio-tracker/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-pnl-allocation/05-CONTEXT.md
@.planning/phases/05-pnl-allocation/05-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add formulaCell helper + BUY-only summary formulas to dcaLogSheet.js</name>
  <files>layout-builder/src/dcaLogSheet.js</files>
  <read_first>
    - layout-builder/src/dcaLogSheet.js (the file being modified — `bandRequests`, `stringCell`/`labelRowRequest`, `SUMMARY_HEADERS`, `FIRST_SUMMARY_ROW`, `LAST_RESERVED_SUMMARY_ROW`, `TX_HEADER_ROW`)
    - layout-builder/src/config.js (DATA_START_ROW=23, MAX_SUMMARY_ROWS=20)
    - layout-builder/src/dashboardSheet.js (the `stringCell` shape to mirror for `formulaCell`)
    - .planning/phases/05-pnl-allocation/05-CONTEXT.md (D-04 summary formula shapes, D-06 IFERROR em-dash)
    - .planning/phases/05-pnl-allocation/05-PATTERNS.md (dcaLogSheet.js section: critical boundary pattern, SUMMARY_HEADERS->TX column mapping)
  </read_first>
  <behavior>
    - Each asset's summary row (rows 2..1+assets.length) emits formula cells in summary columns Total Invested (B), Total Qty (C), Avg Cost (D), Buy Count (E), Last Buy (F), Total Fees (G); column A keeps the existing asset-id label.
    - Every summary formula request range stays at/above row 2 and strictly below row 23 (endRowIndex <= 22 0-based) — the open-ended A{DATA_START_ROW}:A ranges appear ONLY inside formula strings.
    - An asset id with no matching BUY rows yields the em-dash placeholder for Avg Cost (IFERROR), not #DIV/0!.
  </behavior>
  <action>
    Add a `formulaCell(formula)` helper mirroring `stringCell` but emitting `userEnteredValue: { formulaValue: formula }` (formula string begins with "="). Add a `formulaRowRequest(sheetId, row, startCol, formulas)` helper mirroring `labelRowRequest` but mapping each entry through `formulaCell` (use `fields: "userEnteredValue"`, 1-based row/startCol converted to 0-based). In `bandRequests`, after the per-asset label-row loop, emit one formula row per asset at `FIRST_SUMMARY_ROW + i` starting at column 2 (B) with the six BUY-only metrics referencing TX columns (per D-04, PATTERNS.md mapping: Date=A, Asset=B, Type=C, Price=D, Qty=E, Total=F, Fee=G, Net Cost=H). The summary row's own asset id lives in column A (already emitted as a label) — reference it as `$A{row}` in the SUMIFS criterion. Formula shapes (open-ended ranges anchored at DATA_START_ROW=23, every leaf wrapped in IFERROR(…, "—") per D-06):
    - Total Invested (B): `=IFERROR(SUMIFS($H$23:$H,$B$23:$B,$A{row},$C$23:$C,"BUY"),"—")`
    - Total Qty (C): `=IFERROR(SUMIFS($E$23:$E,$B$23:$B,$A{row},$C$23:$C,"BUY"),"—")`
    - Avg Cost (D): `=IFERROR(SUMIFS($H$23:$H,$B$23:$B,$A{row},$C$23:$C,"BUY")/SUMIFS($E$23:$E,$B$23:$B,$A{row},$C$23:$C,"BUY"),"—")`
    - Buy Count (E): `=IFERROR(COUNTIFS($B$23:$B,$A{row},$C$23:$C,"BUY"),"—")`
    - Last Buy (F): `=IFERROR(MAXIFS($A$23:$A,$B$23:$B,$A{row},$C$23:$C,"BUY"),"—")`
    - Total Fees (G): `=IFERROR(SUMIFS($G$23:$G,$B$23:$B,$A{row},$C$23:$C,"BUY"),"—")`
    Use the actual DATA_START_ROW value (23) when composing strings — derive the literal from the imported `DATA_START_ROW` so the strings track the constant (e.g. build a `dataAnchor` = DATA_START_ROW). Keep `build == update` (both call `bandRequests`); do NOT touch the data region. Reserved-but-unused summary rows (assets.length+1..MAX_SUMMARY_ROWS) stay blank — emit NO formula rows for them, matching the existing label-row loop bound. Apply a CURRENCY number format is already in place for cols B/D/G; Last Buy (F) needs a DATE format — add a `numberFormatRequest` for col F over the reserved block using the existing `DATE_FORMAT` constant.
  </action>
  <verify>
    <automated>bun test layout-builder/src/dcaLogSheet.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `dcaLogSheet.js` exports/uses a `formulaCell` helper emitting `userEnteredValue.formulaValue`.
    - JSON.stringify of `dcaLogBuildRequests(0)` contains "formulaValue", "SUMIFS", "COUNTIFS", "MAXIFS", and the `,"BUY"` BUY-only filter.
    - Every emitted request range has `endRowIndex <= 22` (0-based DATA_START_ROW boundary) — no write to row 23+.
    - Avg Cost formula string is wrapped in `IFERROR(…, "—")`.
    - `dcaLogBuildRequests` still equals `dcaLogUpdateRequests` (build == update) for the same registry.
  </acceptance_criteria>
  <done>The DCA Log summary band emits BUY-only SUMIFS/COUNTIFS/MAXIFS formulas per asset, wrapped in IFERROR em-dash, with every request range bounded strictly above the data region.</done>
</task>

<task type="auto">
  <name>Task 2: Invert the skeleton-only assertion in dcaLogSheet.test.js</name>
  <files>layout-builder/src/dcaLogSheet.test.js</files>
  <read_first>
    - layout-builder/src/dcaLogSheet.test.js (the file being modified — especially the "both builders are skeleton-only" test at lines 138-145 and the critical data-region assertion at lines 64-79)
    - layout-builder/src/dcaLogSheet.js (the formulas added in Task 1)
    - .planning/phases/05-pnl-allocation/05-PATTERNS.md (dcaLogSheet.test.js: "skeleton-only test that MUST be updated")
  </read_first>
  <action>
    Replace the `not.toContain("formulaValue")` assertions in the "both builders are skeleton-only (D-08)" test (lines 138-145) with POSITIVE assertions: both build and update serialized output now `toContain("formulaValue")` and `toContain("SUMIFS")`. KEEP the `not.toContain("addConditionalFormatRule")` assertions for BOTH builders — no conditional formatting lands on the DCA Log tab (only the Dashboard, D-07). Rename the test to reflect the inversion (e.g. "summary block now emits formulas; still no conditional formatting"). Add a new test asserting the BUY-only filter and the em-dash empty state: serialized output contains `,"BUY"` and `"—"`. Leave the critical data-region assertion test (lines 64-79) and the boundary-literal test UNCHANGED — they MUST still pass against the new formula requests. Do NOT weaken `DATA_START_ROW_0BASED` (22) or the `endRowIndex <= DATA_START_ROW_0BASED` check.
  </action>
  <verify>
    <automated>bun test layout-builder/</automated>
  </verify>
  <acceptance_criteria>
    - `dcaLogSheet.test.js` asserts `toContain("formulaValue")` for both build and update.
    - `dcaLogSheet.test.js` still asserts `not.toContain("addConditionalFormatRule")` for both build and update.
    - The "NO dcaLogUpdateRequests range touches a row at or below the data region" test still passes (every `endRowIndex <= 22`).
    - The "DATA_START_ROW equals the fixed literal 23" test still passes.
    - `bun test layout-builder/` exits 0.
  </acceptance_criteria>
  <done>The DCA Log test suite asserts the presence of summary formulas while still proving (against the hard literal 23) that no request writes the transaction data region, and still asserting no conditional formatting on this tab.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 01)
New symbols / changes introduced by this plan:
- `formulaCell(formula)` — new helper in `dcaLogSheet.js` emitting `userEnteredValue.formulaValue`.
- `formulaRowRequest(sheetId, row, startCol, formulas)` — new helper in `dcaLogSheet.js` mirroring `labelRowRequest`.
- New summary formula cells per asset (cols B–G, rows 2..1+assets.length): Total Invested, Total Qty, Avg Cost (DCA), Buy Count, Last Buy, Total Fees — all BUY-only, all IFERROR em-dash wrapped.
- New formula ranges: open-ended `A$23:A`/`B$23:B`/`C$23:C`/`E$23:E`/`G$23:G`/`H$23:H` (read-only, inside formula strings).
- New DATE number format applied to summary Last Buy column (F).
- Inverted test assertions in `dcaLogSheet.test.js` (formulas now expected; conditional formatting still absent on this tab).
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user DCA rows → summary SUMIFS | User-entered transaction cells (rows 23+) feed the summary formulas; the only "input" surface for this local builder |
| builder request → Sheets API | Local Node builder writes formulas/formats via a gitignored service-account key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | dcaLogSheet.js summary formula request range | mitigate | Every request range asserted `endRowIndex <= 22` in dcaLogSheet.test.js — irreversible DCA-data-loss guard (LAYOUT-02 / D-06). Open-ended A$23:A ranges appear only inside read-only formula strings, never as a write range. |
| T-05-02 | Tampering | user-entered DCA cells feeding SUMIFS | accept | Spreadsheet formulas evaluate in the user's own sheet against the user's own data; SUMIFS criteria are fixed literals ("BUY") and cell refs, not interpolated user strings. No external/web input. Local single-user tool. |
| T-05-SC | Tampering | npm/bun installs | accept | This plan adds NO new dependencies — extends existing in-file builders only. No install step. |
</threat_model>

<verification>
- `bun test layout-builder/` exits 0 with all DCA Log data-safety and boundary-literal tests passing.
- Serialized DCA Log build requests contain BUY-only SUMIFS/COUNTIFS/MAXIFS formulas wrapped in IFERROR em-dash.
- No DCA Log request range writes at or below row 23.
</verification>

<success_criteria>
- DCA Log summary block computes Total Invested, Total Qty, DCA-weighted Avg Cost, Buy Count, Last Buy, Total Fees per asset from BUY rows only (PNL-02).
- Avg cost is computed only here (single source of truth for Plan 02's Dashboard AvgCost reference).
- An asset with no BUY rows shows em-dash, not #DIV/0!.
- Data-region safety is preserved and proven against the hard literal 23.
</success_criteria>

<output>
Create `.planning/phases/05-pnl-allocation/05-01-SUMMARY.md` when done.
</output>
