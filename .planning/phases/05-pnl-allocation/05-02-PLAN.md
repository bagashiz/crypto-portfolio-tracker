---
phase: 05-pnl-allocation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - layout-builder/src/dashboardSheet.js
  - layout-builder/src/dashboardSheet.test.js
autonomous: true
requirements: [PNL-03, PNL-04, ALLOC-01, ALLOC-02]
must_haves:
  truths:
    - "Zone A shows Asset, Qty, Price, Value(=B*C), Target %, Risk, AvgCost, PnL $, PnL % per asset"
    - "Dashboard AvgCost references the DCA Log summary Avg Cost cell (single source of truth, no duplicated SUMIF)"
    - "PnL $ = Value − Qty*AvgCost and PnL % = (Value − Qty*AvgCost)/(Qty*AvgCost), each IFERROR em-dash wrapped"
    - "PnL $ and PnL % cells are green-filled for gains, red-filled for losses via conditional formatting; re-running --update does not stack duplicate rules"
    - "Zone B shows Asset, Target %, Actual %(=Value/total), Drift(=Actual−Target), Risk; TOTALS row has target sum and blended risk via SUMPRODUCT; Drift is color-flagged when it exceeds tolerance"
    - "The per-venue status block relocates to the right of the new col I with a >=1-col gap"
  artifacts:
    - path: "layout-builder/src/dashboardSheet.js"
      provides: "Widened Zone A/B headers, formula cells, conditional-format rules (idempotent), relocated status block"
      contains: "formulaValue"
    - path: "layout-builder/src/dashboardSheet.test.js"
      provides: "Inverted assertions (formulas + conditional formatting now expected), updated Zone A last-col + status-col anchors"
      contains: "conditionalFormat"
  key_links:
    - from: "dashboardSheet.js Zone A AvgCost cell (col G)"
      to: "DCA Log summary Avg Cost cell"
      via: "cross-sheet formula reference ='DCA Log'!$D{summaryRow}"
      pattern: "DCA Log"
    - from: "dashboardSheet.js PnL columns (H, I) and Drift (Zone B D)"
      to: "conditional-format background-fill rules"
      via: "addConditionalFormatRule with booleanRule (NUMBER_GREATER / NUMBER_LESS)"
      pattern: "addConditionalFormatRule"
---

<objective>
Extend the Dashboard layout builder so Zone A shows unrealized PnL (PNL-03) with green/red conditional formatting (PNL-04) and Zone B shows allocation health — target %, actual %, drift, risk, blended-risk totals (ALLOC-01, ALLOC-02). Widen Zone A to col I, drop the APY % column (D-01), make Value a formula (D-02), reference the DCA Log summary for AvgCost (D-03), reduce Zone B to drop APY %/Monthly Yield (D-05), wrap leaf cells in IFERROR em-dash (D-06), and relocate the status block right of the widened Zone A. This plan CHOOSES the new STATUS_START_COL that Plan 03 (Refresh.ts) must match.

Purpose: Turn the static Dashboard skeleton into a live PnL + allocation view; this is the formula/format layer Phase 2 deferred (D-08).
Output: `dashboardSheet.js` emits formula cells + idempotent conditional-format rules + a relocated status block; `dashboardSheet.test.js` inverts the skeleton-only assertions and bumps the Zone A last-column / status-column anchors.
</objective>

<execution_context>
@/home/bagashiz/Projects/crypto-portfolio-tracker/.claude/gsd-core/workflows/execute-plan.md
@/home/bagashiz/Projects/crypto-portfolio-tracker/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-pnl-allocation/05-CONTEXT.md
@.planning/phases/05-pnl-allocation/05-PATTERNS.md
@layout-builder/src/dcaLogSheet.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Widen Zone A/B, add PnL + allocation formulas, relocate status block</name>
  <files>layout-builder/src/dashboardSheet.js</files>
  <read_first>
    - layout-builder/src/dashboardSheet.js (the file being modified — ZONE_A_HEADERS, ZONE_B_HEADERS, STATUS_START_COL, structuralRequests, stringCell/labelRowRequest, numberFormatRequest, CURRENCY_FORMAT/PERCENT_FORMAT)
    - layout-builder/src/dcaLogSheet.js (DCA Log summary geometry: Avg Cost is summary col D; per-asset summary rows are FIRST_SUMMARY_ROW(2)+i, in assets.json order — needed for the cross-sheet AvgCost reference)
    - layout-builder/src/config.js (DASHBOARD, DCA_LOG sheet names)
    - assets.json (7 assets in order — drives row mapping and target/risk values; apy is vestigial, do NOT read it, D-05)
    - .planning/phases/05-pnl-allocation/05-CONTEXT.md (D-01 column map, D-02 formula shapes, D-03 single-source AvgCost, D-05 Zone B reduction, D-06 IFERROR)
    - .planning/phases/05-pnl-allocation/05-PATTERNS.md (dashboardSheet.js section: formulaCell shape, layout-constant pattern, status-block relocation, cross-runtime coupling)
  </read_first>
  <behavior>
    - Zone A header row is exactly: Asset, Qty, Price, Value, Target %, Risk, AvgCost, PnL $, PnL % (cols A..I); no APY %.
    - Per asset row r (2..1+assets.length): Value(D) `=B{r}*C{r}`; Target %(E) and Risk(F) are static values from assets.json; AvgCost(G) references the DCA Log summary Avg Cost cell for that asset; PnL $(H) `=IFERROR(D{r}-B{r}*G{r},"—")`; PnL %(I) `=IFERROR((D{r}-B{r}*G{r})/(B{r}*G{r}),"—")`.
    - Zone B header row is exactly: Asset, Target %, Actual %, Drift, Risk (cols A..E); no APY %, no Monthly Yield.
    - Zone B per asset: Target %(B) and Risk(E) static; Actual %(C) `=IFERROR(<asset Value>/<Zone A TOTAL Value>,"—")`; Drift(D) `=IFERROR(C{r}-B{r},"—")`. TOTALS row: Target sum (SUM of target col) and blended Risk `=SUMPRODUCT(Risk_range,IFERROR(ActualPct_range,0))`.
    - Status block starts at the chosen STATUS_START_COL (>= col K / 1-based 11) with a >=1-col gap right of col I.
  </behavior>
  <action>
    Add `formulaCell(formula)` (mirrors `stringCell`, emits `userEnteredValue.formulaValue`) and `formulaRowRequest(sheetId, row, startCol, formulas)` (mirrors `labelRowRequest`). Change `ZONE_A_HEADERS` to `["Asset","Qty","Price","Value","Target %","Risk","AvgCost","PnL $","PnL %"]` (drop "APY %", add AvgCost/PnL $/PnL %). Change `ZONE_B_HEADERS` to `["Asset","Target %","Actual %","Drift","Risk"]` (drop "APY %","Monthly Yield"). In `structuralRequests`, for each asset at Zone A row r = ZONE_A_HEADER_ROW+1+i:
    - Value (D): formula `=B{r}*C{r}` (D-02; NOT a refresh-written value).
    - Target % (E): static value from `asset.target` (use a value cell — wrap as a number, not a formula).
    - Risk (F): static value from `asset.risk`.
    - AvgCost (G): cross-sheet reference to the DCA Log summary Avg Cost cell. The DCA Log summary row for asset i is `FIRST_SUMMARY_ROW + i` (i.e. 2+i) and Avg Cost is summary column D — so `='DCA Log'!$D{2+i}` (single source of truth, D-03; quote the sheet name because it contains a space — reference DCA_LOG from config rather than hardcoding). Wrap in IFERROR em-dash.
    - PnL $ (H): `=IFERROR(D{r}-B{r}*G{r},"—")`.
    - PnL % (I): `=IFERROR((D{r}-B{r}*G{r})/(B{r}*G{r}),"—")`.
    Zone A TOTAL row: Value column (D) `=IFERROR(SUM(D{first}:D{last}),"—")` for the total portfolio value (used by Zone B Actual %). For Zone B at row r = ZONE_B_HEADER_ROW+1+i: Target %(B) static `asset.target`; Actual %(C) `=IFERROR(Dashboard Zone A Value of asset i / Zone A TOTAL Value cell,"—")` (reference the same-sheet Zone A Value cell `$D{zoneA_row_of_asset_i}` over the Zone A TOTAL Value cell `$D${zoneATotalRow}`); Drift(D) `=IFERROR(C{r}-B{r},"—")`; Risk(E) static `asset.risk`. Zone B TOTALS row: Target sum `=IFERROR(SUM(B{first}:B{last}),"—")`; blended Risk `=IFERROR(SUMPRODUCT(E{first}:E{last},IFERROR(C{first}:C{last},0)),"—")` (D-06 guard so em-dash text in Actual % is treated as 0). Update number formats: AvgCost(G)+PnL $(H) CURRENCY, PnL %(I) PERCENT for Zone A; Zone B Target/Actual/Drift PERCENT (cols B-D), Risk plain. Relocate the status block: set `STATUS_START_COL = 11` (col K — one-col gap right of new col I=9; document the choice). Keep STATUS_START_ROW/STATUS_HEADERS/STATUS_VENUE_LINES. Bump the re-exported Zone-A-last-col understanding (the test reads STATUS_START_COL; ensure exports still include STATUS_START_COL, STATUS_START_ROW). Keep `dashboardBuildRequests == dashboardUpdateRequests` calling `structuralRequests` (conditional formatting added in Task 2 must preserve build==update for the formula set). Do NOT emit a value into Qty(B)/Price(C) — those stay empty for refreshAll() to write.
  </action>
  <verify>
    <automated>bun test layout-builder/src/dashboardSheet.test.js 2>&1 | head -40</automated>
  </verify>
  <acceptance_criteria>
    - `ZONE_A_HEADERS` equals `["Asset","Qty","Price","Value","Target %","Risk","AvgCost","PnL $","PnL %"]`; `ZONE_B_HEADERS` equals `["Asset","Target %","Actual %","Drift","Risk"]`.
    - JSON.stringify of build requests contains "formulaValue", `=B2*C2` (Value), a `'DCA Log'!$D` cross-sheet AvgCost reference, and `SUMPRODUCT` (blended risk).
    - PnL $/PnL % and Actual %/Drift formula strings are wrapped in `IFERROR(…, "—")`.
    - `STATUS_START_COL` is >= 11 (1-based col K), strictly right of new Zone A last col I (9) with a gap.
    - No request emits a value into Qty(B) or Price(C) per-asset cells.
  </acceptance_criteria>
  <done>Zone A/B headers are widened/reduced per D-01/D-05, per-asset PnL and allocation formulas are emitted with IFERROR em-dash, AvgCost references the DCA Log summary, and the status block is relocated to col K.</done>
</task>

<task type="auto">
  <name>Task 2: Add idempotent green/red conditional-format rules for PnL and Drift</name>
  <files>layout-builder/src/dashboardSheet.js</files>
  <read_first>
    - layout-builder/src/dashboardSheet.js (after Task 1 — structuralRequests, the new Zone A/B geometry, ZONE_A_HEADER_ROW/ZONE_B_HEADER_ROW)
    - .planning/phases/05-pnl-allocation/05-CONTEXT.md (D-07 background-fill conditional formatting; green PnL>0, red PnL<0; Drift color flag)
    - .planning/phases/05-pnl-allocation/05-PATTERNS.md (Conditional-format request shape — NEW, no analog; Conditional-format idempotency shared pattern)
  </read_first>
  <behavior>
    - PnL $ (col H) and PnL % (col I) per-asset rows get green background fill when value > 0, red when value < 0, no fill at 0 or em-dash.
    - Drift (Zone B col D) per-asset rows get red background fill when |drift| >= tolerance (default 0.05 absolute = 5 percentage points).
    - Running --update twice does not stack duplicate conditional-format rules (idempotent).
  </behavior>
  <action>
    Add an `addConditionalFormatRule` request builder (no existing analog — use Sheets v4 shape: `addConditionalFormatRule: { rule: { ranges: [{sheetId, startRowIndex, endRowIndex, startColumnIndex, endColumnIndex}], booleanRule: { condition: { type: "NUMBER_GREATER", values: [{ userEnteredValue: "0" }] }, format: { backgroundColor: {green fill} } } }, index } }`). Emit, over the Zone A per-asset row span (rows ZONE_A_HEADER_ROW+1 .. zoneATotalRow exclusive of TOTAL, cols H and I — 0-based startColumnIndex 7, endColumnIndex 9): a green rule `NUMBER_GREATER 0` and a red rule `NUMBER_LESS 0`. For Drift (Zone B col D, 0-based col index 3) over the Zone B per-asset rows: a red rule using `NUMBER_GREATER` on `=ABS(D{firstZoneBrow})>=0.05` via a `CUSTOM_FORMULA` boolean condition (absolute drift tolerance; threshold is Claude's discretion per D-07 — default 0.05). Use distinct light green / light red `backgroundColor` RGB objects. IDEMPOTENCY (D-07): conditional-format rules STACK on naive re-add. Before emitting the `addConditionalFormatRule` batch, emit `deleteConditionalFormatRule` requests for the managed rule indices on the Dashboard sheet (delete index 0 repeatedly for the count of managed rules, since each delete shifts indices down) — OR emit deletes for a fixed managed index range descending. Document the chosen approach inline. The deletes must be no-ops/safe on first --build (a sheet with no rules): guard by deleting only down to 0, and rely on the build/update flow tolerating "no rule at index" — prefer emitting deletes for indices [N-1 .. 0] descending where N is the managed rule count, AND make this idempotency handled in code such that re-running update yields the same final rule count. Both `dashboardBuildRequests` and `dashboardUpdateRequests` must include these rules (D-07: applied in BOTH).
  </action>
  <verify>
    <automated>bun test layout-builder/src/dashboardSheet.test.js 2>&1 | head -40</automated>
  </verify>
  <acceptance_criteria>
    - JSON.stringify of build requests contains "addConditionalFormatRule", "NUMBER_GREATER", "NUMBER_LESS", and "backgroundColor".
    - Conditional-format rules target cols H+I (PnL) over Zone A asset rows and col D over Zone B asset rows.
    - Both `dashboardBuildRequests` and `dashboardUpdateRequests` include conditional-format rules.
    - The builder emits `deleteConditionalFormatRule` (or an equivalent guard) so re-running update does not grow the managed rule count.
  </acceptance_criteria>
  <done>Green/red background-fill conditional-format rules are emitted for PnL $/PnL % and a tolerance-based red flag for Drift, with delete-then-add idempotency so --update never stacks duplicates.</done>
</task>

<task type="auto">
  <name>Task 3: Invert dashboard skeleton assertions; bump Zone A last-col + status-col anchors</name>
  <files>layout-builder/src/dashboardSheet.test.js</files>
  <read_first>
    - layout-builder/src/dashboardSheet.test.js (the file being modified — the two "skeleton-only" tests at lines 51-61, the ZONE_A_LAST_COL_0BASED=6 anchor at line 107, the status-block column-anchoring tests at lines 127-181)
    - layout-builder/src/dashboardSheet.js (after Tasks 1-2 — new headers, new STATUS_START_COL=11, conditional-format rules)
    - .planning/phases/05-pnl-allocation/05-PATTERNS.md (Cross-runtime status-block coupling: ZONE_A_LAST_COL_0BASED must bump to reflect Zone A ending at col I = 0-based 8)
  </read_first>
  <action>
    Replace the two "skeleton-only: no formulaValue, no addConditionalFormatRule (D-08)" tests (build + update) with POSITIVE assertions: both `toContain("formulaValue")` and `toContain("addConditionalFormatRule")`. Rename them to reflect the inversion. Bump `ZONE_A_LAST_COL_0BASED` from 6 (col G) to 8 (col I) — Zone A now ends at col I (1-based 9, 0-based 8). The status-block anchoring tests assert status requests start strictly right of `ZONE_A_LAST_COL_0BASED`; with STATUS_START_COL=11 (0-based 10 > 8) they pass — verify and adjust the `status block never intersects any zone request` test which compares zone label/format extents against `statusCol0`: ensure the new conditional-format ranges (cols H/I = 0-based 7/8) are still strictly left of statusCol0 (10) — they are, but if the intersection test iterates `addConditionalFormatRule` ranges, extend its range-extraction to skip or bound-check those rule ranges (endColumnIndex <= statusCol0). Add a positive test: serialized build output contains the cross-sheet AvgCost reference `'DCA Log'!$D` and `SUMPRODUCT`. Keep all data-safety/column-anchoring invariants passing.
  </action>
  <verify>
    <automated>bun test layout-builder/</automated>
  </verify>
  <acceptance_criteria>
    - `dashboardSheet.test.js` asserts `toContain("formulaValue")` and `toContain("conditionalFormat")` (via "addConditionalFormatRule") for both build and update.
    - `ZONE_A_LAST_COL_0BASED` is 8 (col I).
    - The status-block column-anchoring tests pass with STATUS_START_COL=11.
    - A test asserts the cross-sheet AvgCost reference (`'DCA Log'!$D`) and `SUMPRODUCT` appear in build output.
    - `bun test layout-builder/` exits 0.
  </acceptance_criteria>
  <done>The Dashboard test suite asserts formulas and conditional formatting are present, with the Zone A last-column and status-column anchors updated to the widened geometry; all column-non-collision invariants still pass.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)
New symbols / changes introduced by this plan:
- `formulaCell(formula)` and `formulaRowRequest(...)` — new helpers in `dashboardSheet.js` (mirror `stringCell`/`labelRowRequest`).
- `addConditionalFormatRule` request builder + `deleteConditionalFormatRule` idempotency emission — genuinely NEW construct (no prior analog in repo).
- Changed constant: `ZONE_A_HEADERS` (drop APY %, add AvgCost/PnL $/PnL %), `ZONE_B_HEADERS` (drop APY %/Monthly Yield).
- Changed constant: `STATUS_START_COL` 9 → 11 (col K) — the cross-runtime geometry value Plan 03 must match (`Refresh.ts STATUS_LASTUPDATED_COL = STATUS_START_COL + 1 = 12`).
- New formula cells: Zone A Value/AvgCost/PnL $/PnL %, Zone A TOTAL Value; Zone B Actual %/Drift, Zone B TOTALS target sum + blended-risk SUMPRODUCT.
- New cross-sheet reference: Dashboard AvgCost (G) → `'DCA Log'!$D{2+i}`.
- New conditional-format rules: green/red PnL (cols H, I) + red Drift tolerance flag (Zone B col D).
- Changed test anchor: `ZONE_A_LAST_COL_0BASED` 6 → 8; inverted skeleton assertions.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user Qty/Price + DCA AvgCost → Dashboard formulas | refreshAll-written Qty/Price and the DCA Log summary AvgCost feed Value/PnL/allocation formulas |
| builder request → Sheets API | Local Node builder writes formulas/formats/conditional-format rules via a gitignored service-account key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Tampering | conditional-format rule re-add on --update | mitigate | Emit deleteConditionalFormatRule for managed indices before addConditionalFormatRule so --update is idempotent and never stacks duplicate rules (D-07); assert managed rule count is stable across re-runs. |
| T-05-04 | Tampering | Dashboard is on a tab with no protected data region | accept | The Dashboard has no user-entered data region (only refreshAll-written Qty/Price + formulas); build==update is safe by construction. No irreversible-data-loss surface here (unlike the DCA Log). |
| T-05-05 | Denial of Service | #VALUE/#DIV cascade from missing AvgCost / em-dash text | mitigate | Every leaf formula wrapped IFERROR(…,"—"); blended-risk SUMPRODUCT guards Actual% text via IFERROR(…,0) (D-06) so em-dash never propagates into aggregates. |
| T-05-SC | Tampering | npm/bun installs | accept | This plan adds NO new dependencies — extends existing in-file builders only. No install step. |
</threat_model>

<verification>
- `bun test layout-builder/` exits 0; all column-anchoring + status-non-collision invariants pass with the widened geometry.
- Serialized Dashboard build requests contain PnL formulas, cross-sheet AvgCost reference, SUMPRODUCT blended risk, and green/red conditional-format rules.
- Re-running update yields a stable conditional-format rule count (idempotent).
</verification>

<success_criteria>
- Dashboard shows unrealized PnL in USD and % per asset (PNL-03) computed as Value − Qty*AvgCost.
- PnL cells are green for gains / red for losses via conditional formatting (PNL-04).
- Allocation zone shows target %, actual %, drift, risk per asset plus a TOTALS row with target sum and blended risk via SUMPRODUCT (ALLOC-01, ALLOC-02).
- Status block relocated to col K; STATUS_START_COL=11 is the value Plan 03 matches.
</success_criteria>

<output>
Create `.planning/phases/05-pnl-allocation/05-02-SUMMARY.md` when done.
</output>
