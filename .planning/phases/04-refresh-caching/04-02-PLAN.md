---
phase: 04-refresh-caching
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - layout-builder/src/dashboardSheet.js
  - layout-builder/src/dashboardSheet.test.js
autonomous: true
requirements: [REFRESH-04]
must_haves:
  truths:
    - "The Dashboard layout stamps a 2-line per-venue status block (Hyperliquid + Solana/Jupiter), each with a LastUpdated and a Stale? label"
    - "The status block is column-anchored in free columns to the right of Zone A and never overlaps Zone A (cols A-G) or Zone B"
    - "The status labels are applied by both dashboardBuildRequests and dashboardUpdateRequests (build and update)"
    - "The status block survives the MAX_ZONE_A_ASSET_ROWS registry-growth guard because it is column-anchored, not row-floating"
  artifacts:
    - path: "layout-builder/src/dashboardSheet.js"
      provides: "static status-block labels via labelRowRequest in structuralRequests"
      contains: "LastUpdated"
    - path: "layout-builder/src/dashboardSheet.test.js"
      provides: "offline assertions that the status labels exist, are column-anchored right of Zone A, and don't collide with zones"
  key_links:
    - from: "layout-builder/src/dashboardSheet.js"
      to: "labelRowRequest"
      via: "compose existing helper for status labels (no raw batchUpdate)"
      pattern: "labelRowRequest"
    - from: "structuralRequests"
      to: "dashboardBuildRequests / dashboardUpdateRequests"
      via: "both delegate to structuralRequests so labels apply to build + update"
      pattern: "structuralRequests"
---

<objective>
Stamp the STATIC labels for the per-venue refresh status block into the Dashboard layout: two lines (Hyperliquid, Solana/Jupiter), each with a `LastUpdated` and a `Stale?` header, placed in free columns to the right of Zone A. This is the build-time half of the D-05 runtime split — the layout builder owns all static structure; Apps Script `refreshAll()` (Plan 01) writes only the dynamic timestamp + Stale? values into the adjacent cells.

Purpose: REFRESH-04 — the glanceable per-venue staleness indicator (D-04/D-05/D-06). A user can see at a glance which venue is behind.
Output: status-label requests in `dashboardSheet.js` (applied by both build and update) plus offline tests proving placement and non-collision.
</objective>

<execution_context>
@/home/bagashiz/Projects/crypto-portfolio-tracker/.claude/gsd-core/workflows/execute-plan.md
@/home/bagashiz/Projects/crypto-portfolio-tracker/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-refresh-caching/04-CONTEXT.md
@.planning/phases/04-refresh-caching/04-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
</context>

<artifacts_this_phase_produces>
This plan produces (Plan 01's refreshAll() writes the dynamic values into the cells adjacent to these labels):
- Static status-block label strings: venue names `Hyperliquid` and `Solana/Jupiter` (or `Solana`), and the `LastUpdated` / `Stale?` headers
- Status-block placement: column-anchored, top-right of the Dashboard (cols I–K, rows 1–3 per D-06; exact columns are Claude's discretion as long as they sit right of Zone A cols A–G and don't overlap Zone B)
- Both `dashboardBuildRequests` and `dashboardUpdateRequests` now emit these labels (via shared `structuralRequests`)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add static per-venue status-block labels to the Dashboard layout</name>
  <files>layout-builder/src/dashboardSheet.js</files>
  <read_first>
    - layout-builder/src/dashboardSheet.js (lines 17-32 layout constants + MAX_ZONE_A_ASSET_ROWS guard; lines 46-58 labelRowRequest helper; lines 93-140 structuralRequests where new requests are pushed; lines 145-153 build/update delegation)
    - .planning/phases/04-refresh-caching/04-CONTEXT.md (D-04 exactly-2-lines, D-05 layout-builder owns static labels, D-06 top-right column-anchored placement)
    - .planning/phases/04-refresh-caching/04-PATTERNS.md (section "layout-builder/src/dashboardSheet.js" — reuse labelRowRequest, push into structuralRequests, placement constraint)
    - .planning/phases/02-layout-builder/02-CONTEXT.md (Zone A↔B boundary model the placement must respect — read only the layout/boundary sections)
  </read_first>
  <action>
    Add named layout constants for the status block (mirror the existing `ZONE_A_HEADER_ROW`/`ZONE_B_HEADERS` const style): a `STATUS_START_COL` (a 1-based column to the right of Zone A's cols A–G — e.g. col I = 9) and `STATUS_START_ROW` (e.g. row 1), plus the static label text. Compose the block using the EXISTING `labelRowRequest` helper (never hand-build a raw batchUpdate request — CONVENTIONS single-source helper rule). The block is exactly 2 venue lines (D-04): one for Hyperliquid, one for Solana/Jupiter; each line carries the static `LastUpdated` and `Stale?` header labels, leaving the adjacent value cells empty for refreshAll() to fill. Suggested shape: a small header label row (`Status`, `LastUpdated`, `Stale?`) at STATUS_START_ROW, then a `Hyperliquid` row and a `Solana/Jupiter` row beneath — final geometry is Claude's discretion within cols I–K, rows 1–3 (D-06).
    Push the new label requests into the `requests` array inside `structuralRequests` (alongside the Zone A/B labels) so BOTH `dashboardBuildRequests` and `dashboardUpdateRequests` emit them automatically. Keep the block COLUMN-anchored (fixed columns), so the `MAX_ZONE_A_ASSET_ROWS` guard — which only shifts ROWS — can never collide with it. Do NOT add formulas or conditional formatting (Phase 5). Document the exact columns/rows chosen in a brief inline comment so Plan 01's refreshAll() writes the dynamic values into the matching cells.
  </action>
  <verify>
    <automated>grep -E 'LastUpdated|Stale' layout-builder/src/dashboardSheet.js | grep -v '^#'</automated>
  </verify>
  <acceptance_criteria>
    - dashboardSheet.js declares a column-anchored status block (named STATUS_START_COL/STATUS_START_ROW constants) placed at a column >= 8 (col H/I, i.e. right of Zone A cols A–G).
    - The block carries exactly two venue lines (Hyperliquid, Solana/Jupiter) plus LastUpdated and Stale? labels, composed via labelRowRequest (no raw updateCells literals added).
    - The label requests are pushed inside structuralRequests (so both build and update emit them).
    - No formulaValue or addConditionalFormatRule added.
  </acceptance_criteria>
  <done>The Dashboard layout stamps the 2-line per-venue static status block in fixed top-right columns, applied by both --build and --update.</done>
</task>

<task type="auto">
  <name>Task 2: Offline tests — status labels exist, are column-anchored, and don't collide with zones</name>
  <files>layout-builder/src/dashboardSheet.test.js</files>
  <read_first>
    - layout-builder/src/dashboardSheet.test.js (lines 1-90 — existing offline test patterns: import testEnv, bun:test, assert request arrays, the no-collision invariant test, the skeleton-only/no-formula assertions to mirror)
    - layout-builder/src/dashboardSheet.js (the status constants + requests added in Task 1, to assert against)
    - apps-script/src/parsers.test.ts (only if a fixture-style example helps; primary analog is dashboardSheet.test.js)
  </read_first>
  <action>
    Add bun:test cases mirroring the existing offline tests (no network, no credentials):
    - assert dashboardBuildRequests output contains label requests whose values include `LastUpdated`, `Stale?`, `Hyperliquid`, and `Solana` (or `Solana/Jupiter`).
    - assert the status-block requests' `start.columnIndex` is >= Zone A's last column (0-based >= 7, i.e. col H or further right) — proving column-anchoring right of Zone A.
    - assert dashboardUpdateRequests emits the same status labels (build == update for this static structure).
    - assert the status block does NOT overlap Zone B: its row indices stay above ZONE_B_HEADER_ROW, AND extend the existing full-capacity no-collision test (or add a sibling) to confirm that even at MAX_ZONE_A_ASSET_ROWS assets the status block columns never intersect any Zone A/B label request range.
    - assert no formulaValue / addConditionalFormatRule is introduced by the status requests (extend the existing skeleton-only assertion).
  </action>
  <verify>
    <automated>cd layout-builder && bun test src/dashboardSheet.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `bun test src/dashboardSheet.test.js` passes including the new status-label cases.
    - A test asserts the status-block column index is right of Zone A (0-based >= 7).
    - A test asserts both build and update emit the status labels.
    - A test asserts non-collision with Zone B at full registry capacity.
    - A test asserts the status requests stay skeleton-only (no formulas/conditional formatting).
  </acceptance_criteria>
  <done>Offline tests prove the status labels exist, are column-anchored right of Zone A, apply on build and update, and never collide with the zones even at max registry size.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| layout builder (Node) → Google Sheet | Service-account batchUpdate writes static structure to the Dashboard via the Sheets API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Tampering | status block overwriting Zone A/B or DCA data | mitigate | Block is column-anchored right of Zone A (cols A–G) and above Zone B; a unit test asserts non-collision at MAX_ZONE_A_ASSET_ROWS so registry growth can never make the labels stamp onto zone data (mirrors the LAYOUT-02 boundary-safety discipline) |
| T-04-06 | Denial of Service | --update wiping refreshAll-written dynamic values | accept | --update re-stamps only the STATIC labels (empty adjacent value cells); the next refreshAll run (≤5 min) repopulates the dynamic timestamp/Stale? values — transient, self-healing, no data loss (DCA Log data region is untouched, separate tab) |
| T-04-SC | Tampering | npm installs | accept | No new packages added; reuses existing googleapis-based layout builder and local helpers only. No package legitimacy gate needed |
</threat_model>

<verification>
- `cd layout-builder && bun test` — all existing dashboard/dcaLog/config tests AND the new status-label tests pass.
- grep confirms LastUpdated/Stale? labels and column-anchored placement constants in dashboardSheet.js.
</verification>

<success_criteria>
- REFRESH-04 (build-time half): the Dashboard layout stamps a 2-line per-venue status block (Hyperliquid + Solana/Jupiter), each with LastUpdated and Stale? labels, in fixed top-right columns, applied by both --build and --update, provably non-colliding with Zones A/B at any registry size up to the guard.
</success_criteria>

<output>
Create `.planning/phases/04-refresh-caching/04-02-SUMMARY.md` when done.

Note: materializing these labels in the live sheet requires a one-time `layout-builder --update` run — performed at the Plan 03 live-verify checkpoint, not here.
</output>
