---
phase: 04-refresh-caching
plan: 03
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified: []
autonomous: false
requirements: [REFRESH-01, REFRESH-02, REFRESH-03, REFRESH-04]
user_setup:
  - service: google-apps-script
    why: "Deploy the new refresh layer and install the live time-driven trigger"
    dashboard_config:
      - task: "Re-authorize the script when the new spreadsheets + script.scriptapp OAuth scopes prompt on first run"
        location: "Apps Script editor → run refreshAll/installTrigger → OAuth consent"
  - service: google-sheets
    why: "Materialize the new static status-block labels in the live Dashboard"
    dashboard_config:
      - task: "Run `layout-builder --update` once to stamp the status labels"
        location: "local terminal (service-account credentials)"
must_haves:
  truths:
    - "A one-time `layout-builder --update` materializes the status-block labels in the live Dashboard without altering DCA Log data rows"
    - "Deployed refreshAll() writes live prices/balances into Zone A Qty/Price cells and the per-venue status cells, leaving Value (col D) and Target/Risk/APY (cols E-G) untouched"
    - "installTrigger() creates exactly one refreshAll time-driven trigger; running it twice does not stack duplicates; removeTrigger() removes it"
    - "Simulating one venue's failure keeps that venue's last-good values, sets its Stale?=TRUE and freezes LastUpdated, while the healthy venue stays fresh"
  artifacts: []
  key_links:
    - from: "deployed refreshAll()"
      to: "Dashboard Zone A Qty/Price + status cells"
      via: "single setValues + status writes, verified live"
      pattern: "live editor run"
---

<objective>
Deploy the Phase 4 refresh layer to the live container-bound script, materialize the new status labels in the sheet, install the time-driven trigger, and verify end-to-end behavior against the user's real wallets — including a simulated partial failure to confirm graceful degradation. This is the live-integration gate: the prior plans built and unit-tested the logic; this plan proves it works in the deployed environment (REFRESH-01..04).

Purpose: confirm the four Phase 4 success criteria hold against the real spreadsheet and real provider responses, catching live-only issues (status-cell alignment, OAuth scope grants, trigger creation, real degradation) the way Phase 3 caught the HL index-alignment bug at its live checkpoint.
Output: a deployed, trigger-driven, self-refreshing Dashboard verified by the user.
</objective>

<execution_context>
@/home/bagashiz/Projects/crypto-portfolio-tracker/.claude/gsd-core/workflows/execute-plan.md
@/home/bagashiz/Projects/crypto-portfolio-tracker/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-refresh-caching/04-CONTEXT.md
@.planning/phases/04-refresh-caching/04-01-PLAN.md
@.planning/phases/04-refresh-caching/04-02-PLAN.md
</context>

<artifacts_this_phase_produces>
No new source symbols. This plan deploys and live-verifies the artifacts produced by Plans 01 and 02:
- Live `--update` run materializes the status-block labels (Plan 02)
- Deployed `refreshAll`, `installTrigger`, `removeTrigger` globals exercised in the editor (Plan 01)
- A live `PRICES_ALL` blob populated after the first successful run
- One installed time-driven trigger
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Build, deploy, and materialize the status labels</name>
  <files></files>
  <read_first>
    - apps-script/package.json (the `deploy` script: bun run build && cp appsscript.json dist/appsscript.json && clasp push)
    - apps-script/README.md (deploy/clasp notes)
    - layout-builder/src/index.js (the --update CLI entry the user runs to stamp the status labels)
    - .planning/phases/04-refresh-caching/04-CONTEXT.md (D-05 — one-time --update requirement)
  </read_first>
  <action>
    Run the apps-script deploy: `cd apps-script && bun run deploy` (builds the IIFE bundle, appends the top-level shims, copies appsscript.json with the new scopes into dist/, and `clasp push`es). If clasp returns an auth error, surface it as a dynamic auth gate for the user to `clasp login`, then retry — do not pre-plan the auth step.
    Run the layout-builder status-label materialization: `cd layout-builder && node src/index.js --update` (or the package's update script) so the new static status labels (Plan 02) appear in the live Dashboard. This --update is idempotent and must not touch DCA Log data rows (LAYOUT-02 guarantee already in place).
    Report the clasp push result and confirm the --update completed without error. Do NOT run refreshAll yet (that's the human-verify task, which also handles the new-scope OAuth consent).
  </action>
  <verify>
    <automated>cd apps-script && grep -E '^function refreshAll' dist/Code.js && grep -q 'auth/spreadsheets' dist/appsscript.json</automated>
  </verify>
  <acceptance_criteria>
    - `bun run deploy` completes (clasp push succeeds, or an auth gate is surfaced and resolved then retried).
    - dist/appsscript.json (the pushed copy) contains the two new scopes.
    - `layout-builder --update` completes without error and reports no change to DCA Log data rows.
  </acceptance_criteria>
  <done>The new bundle is pushed and the status labels are materialized in the live Dashboard.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live-verify refresh, single batched write, trigger lifecycle, and graceful degradation</name>
  <action>Pause for the user to perform the live verification steps in how-to-verify against the deployed script and real wallets. Do not auto-advance; wait for the resume-signal. On a reported discrepancy, fix in Plan 01/02 source, redeploy, and re-verify.</action>
  <what-built>
    A deployed refreshAll() that fetches both venues live and writes prices+balances to Zone A (Qty col B, Price col C) in a single setValues, maintains the PRICES_ALL last-good blob, and writes per-venue LastUpdated/Stale? status cells; plus installTrigger()/removeTrigger() for the 5-minute time-driven trigger. The new spreadsheets + script.scriptapp OAuth scopes are now requested.
  </what-built>
  <how-to-verify>
    1. In the Apps Script editor, select `refreshAll` and Run. Approve the new OAuth scopes (spreadsheets, script.scriptapp) at the consent prompt. Confirm: Zone A Qty (col B) and Price (col C) cells populate with live numbers for all 7 assets in registry order; Value (col D) and Target/Risk/APY (cols E–G) remain untouched; the two status lines (Hyperliquid, Solana/Jupiter) show a fresh LastUpdated timestamp (Asia/Jakarta) and Stale?=FALSE.
    2. Run `refreshAll` a second time — confirm values refresh (genuinely live each run per D-01, not a frozen cache) and timestamps advance.
    3. Simulate a partial failure (e.g. temporarily set an invalid JUP_API_KEY Script Property, OR a bad SOL_WALLET_ADDRESS) and Run `refreshAll`. Confirm: Solana/Jupiter rows KEEP their last-good Qty/Price (no blank, no #N/A/ERR in the cells), that venue's Stale?=TRUE and its LastUpdated is frozen at the prior time, WHILE Hyperliquid rows refresh and stay Stale?=FALSE. Then restore the valid property and Run again — confirm Solana self-heals (Stale? back to FALSE, LastUpdated advances).
    4. Run `installTrigger`. In the editor's Triggers panel confirm exactly ONE time-driven trigger for refreshAll at ~5-minute cadence. Run `installTrigger` AGAIN and confirm still exactly one (idempotent, no duplicate). Run `removeTrigger` and confirm the trigger is gone. Re-run `installTrigger` to leave the live trigger installed for normal operation.
    5. Confirm the DCA Log tab data rows are unchanged after the --update + refresh runs.
  </how-to-verify>
  <resume-signal>Type "approved" once all five checks pass, or describe the discrepancy (e.g. wrong status-cell alignment, a venue blanking on failure, duplicate triggers) for a fix-and-redeploy loop.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor user → OAuth consent | First run of the new scopes requires explicit user authorization (spreadsheets write + trigger management) |
| live provider responses → Dashboard | Real Hyperliquid/Jupiter responses written to the live sheet, including under induced failure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Tampering | live refresh corrupting good cells under failure | mitigate | The simulated-failure step (check 3) explicitly verifies a failed venue never blanks or error-marks its cells and never stales the healthy venue — the live proof of D-03/D-07 |
| T-04-08 | Denial of Service | duplicate live triggers exhausting quota | mitigate | Check 4 verifies running installTrigger twice yields exactly one trigger (live idempotency proof of D-09) |
| T-04-09 | Elevation of Privilege | OAuth scope grant | accept | User explicitly consents to spreadsheets + script.scriptapp at the live prompt; minimum scopes for a container-bound read-only personal tracker |
| T-04-SC | Tampering | npm installs | accept | No package installs in this plan; deploy uses existing clasp + googleapis toolchains only |
</threat_model>

<verification>
- Live editor run of refreshAll populates Zone A Qty/Price + status cells, leaves Value/Target/Risk/APY untouched.
- Induced single-venue failure keeps last-good + flags Stale?=TRUE for only that venue; self-heals next run.
- installTrigger is idempotent (exactly one trigger after repeated installs); removeTrigger removes it.
- DCA Log data rows unchanged after --update + refresh.
</verification>

<success_criteria>
- REFRESH-01: installTrigger/removeTrigger manage exactly one live time-driven refreshAll trigger; idempotent install confirmed in the Triggers panel.
- REFRESH-02: a single live refreshAll run writes all venue data in one batched setValues (Zone A Qty/Price only).
- REFRESH-03: PRICES_ALL blob populated after a live fetch and served as last-good on the induced-failure run.
- REFRESH-04: induced partial failure keeps last-good values, flags Stale?/LastUpdated per venue, never overwrites good data with an error, and self-heals.
</success_criteria>

<output>
Create `.planning/phases/04-refresh-caching/04-03-SUMMARY.md` when done.
</output>
