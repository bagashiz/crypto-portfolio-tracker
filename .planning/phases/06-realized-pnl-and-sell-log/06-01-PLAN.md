---
phase: 06-realized-pnl-and-sell-log
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - layout-builder/src/config.js
  - layout-builder/src/dcaLogSheet.js
  - layout-builder/src/dcaLogSheet.test.js
autonomous: true
requirements: [PNL-05]
must_haves:
  truths:
    - "Each SELL row in the data region shows a per-row realized PnL value via a single header-cell spill formula; BUY/blank rows show empty"
    - "The summary band shows, per asset, Sold Qty, Net Proceeds, Realized $, and Realized % derived from SELL rows only"
    - "A portfolio Total Realized PnL cell sums the per-asset Realized $ values"
    - "Realized $ and Realized % summary cells get green/red background-fill conditional formatting (>0 / <0)"
    - "The Phase 5 BUY-only avg-cost summary formulas are byte-for-byte unchanged"
    - "No emitted request range addresses any row at or below DATA_START_ROW (row 23); the data region is never written"
    - "The tab name constant DCA_LOG resolves to the user-visible title 'Transaction Log'"
  artifacts:
    - path: "layout-builder/src/config.js"
      provides: "DCA_LOG renamed to 'Transaction Log'; new DCA_LOG_LEGACY = 'DCA Log' constant"
      contains: "DCA_LOG_LEGACY"
    - path: "layout-builder/src/dcaLogSheet.js"
      provides: "Realized spill header formula (BYROW), realized summary metrics, conditional-format rules, PERCENT_FORMAT, dcaLogConditionalPreClearRequests export"
      contains: "BYROW"
    - path: "layout-builder/src/dcaLogSheet.test.js"
      provides: "Inverted conditional-format assertion + realized formula/spill/range assertions"
      contains: "addConditionalFormatRule"
  key_links:
    - from: "layout-builder/src/dcaLogSheet.js realized summary metrics"
      to: "the col J 'Realized' helper column"
      via: "SUMIFS($J$23:$J,...,\"SELL\")"
      pattern: "SUMIFS.*\\$J\\$23"
    - from: "realized spill header formula"
      to: "the BUY-weighted running average over BUY rows dated <= the SELL row date"
      via: "BYROW(A23:I23, LAMBDA(...SUMIFS(...,\"BUY\",A23:A,\"<=\"&d)...))"
      pattern: "BYROW"
---

<objective>
Add SELL semantics and realized PnL to the Transaction Log builder (`dcaLogSheet.js`), rename
the tab constant to "Transaction Log" (D-07 config side), and update the offline test suite —
all without touching the protected data region (rows >= DATA_START_ROW = 23) and without
altering the Phase 5 BUY-only avg-cost summary formulas.

Purpose: Deliver PNL-05 — the user can log SELL rows and see per-asset realized PnL (net
proceeds vs DCA-weighted at-sale cost basis) in the summary block.
Output: Extended `config.js` constants, extended `dcaLogSheet.js` request-builder with the
realized spill column / summary metrics / conditional formatting, and an updated test suite
asserting the new formulas, the inverted conditional-format expectation, and the unchanged
never-write-the-data-region guard.
</objective>

<execution_context>
@/home/bagashiz/Projects/crypto-portfolio-tracker/.claude/gsd-core/workflows/execute-plan.md
@/home/bagashiz/Projects/crypto-portfolio-tracker/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-realized-pnl-and-sell-log/06-CONTEXT.md
@.planning/phases/06-realized-pnl-and-sell-log/06-RESEARCH.md
@.planning/phases/06-realized-pnl-and-sell-log/06-PATTERNS.md
</context>

<artifacts_this_phase_produces>
Symbols/strings introduced or changed by THIS plan (downstream consumers in Plan 02 depend on
the constants + the pre-clear export):

- `config.js`: `DCA_LOG` value changes from `"DCA Log"` to `"Transaction Log"` (symbol name
  unchanged); NEW export `DCA_LOG_LEGACY = "DCA Log"`.
- `dcaLogSheet.js`:
  - `TX_HEADERS` gains a 10th entry: `"Realized"` (col J = column index 10).
  - `SUMMARY_HEADERS` gains: `"Sold Qty"`, `"Net Proceeds"`, `"Realized $"`, `"Realized %"`,
    `"Total Realized"`.
  - NEW number-format constant `PERCENT_FORMAT = { type: "PERCENT", pattern: "0.00%" }`.
  - NEW realized spill header formula string (begins `=BYROW(A23:I23, LAMBDA(`).
  - NEW per-asset realized summary block emitted via existing `formulaRowRequest` at summary
    `startCol` = 8 (col H): Sold Qty (H), Net Proceeds (I), Realized $ (J), Realized % (K).
  - NEW portfolio Total Realized cell (`SUM` of per-asset Realized $ summary column J).
  - NEW conditional-format helpers copied from `dashboardSheet.js`:
    `addConditionalFormatRuleRequest`, `deleteConditionalFormatRuleRequest`, `GREEN_FILL`,
    `RED_FILL`, `DCA_LOG_MANAGED_RULE_COUNT` (= 2: green Realized>0, red Realized<0),
    and NEW exported `dcaLogConditionalPreClearRequests(sheetId)`.
  - `bandRequests` gains a `preClearConditionalRules` param (false on build, true on update),
    mirroring `dashboardSheet.js structuralRequests`.
- `dcaLogSheet.test.js`: `EXPECTED_HEADERS` gains `"Realized"`; the "STILL no conditional
  formatting" assertion (lines 146-148) is INVERTED; new realized-formula/spill/range tests.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rename constant, extend headers/formats, emit realized summary metrics + spill helper</name>
  <files>layout-builder/src/config.js, layout-builder/src/dcaLogSheet.js</files>
  <read_first>
    - layout-builder/src/config.js (FULL) — esp. lines 39-41 (DASHBOARD/DCA_LOG constants),
      lines 70/74 (MAX_SUMMARY_ROWS=20, DATA_START_ROW=23 — UNCHANGED).
    - layout-builder/src/dcaLogSheet.js (FULL) — esp. lines 44-59 (SUMMARY_HEADERS, TX_HEADERS,
      CURRENCY_FORMAT, DATE_FORMAT, TX_HEADER_ROW=22), lines 86-94 (formulaRowRequest helper),
      lines 96-110 (numberFormatRequest), lines 170-193 (BUY summary loop — DO NOT MODIFY,
      copy its shape), lines 195-214 (number-format range emission).
    - .planning/phases/06-realized-pnl-and-sell-log/06-RESEARCH.md lines 230-311 (Code Examples:
      the BYROW whole-row spill template lines 266-277, the per-asset realized SUMIFS block
      lines 281-303, the portfolio Total Realized cell lines 305-311) and Pitfall 4 (lines
      214-218: Realized % denominator = NetProceeds − Realized$, NOT current Avg Cost).
    - .planning/phases/06-realized-pnl-and-sell-log/06-PATTERNS.md lines 37-79 (Patterns 1-3:
      summary formula loop, formulaRowRequest, header/format extension; PERCENT_FORMAT source
      = dashboardSheet.js line 76).
  </read_first>
  <action>
    In config.js: change the `DCA_LOG` export VALUE from "DCA Log" to "Transaction Log" (keep
    the symbol name `DCA_LOG`). Add a new export immediately after it:
    `DCA_LOG_LEGACY = "DCA Log"` with a one-line comment noting it is the OLD title used only
    for rename discovery (per D-07 / RESEARCH Pitfall 2). Do NOT change MAX_SUMMARY_ROWS or
    DATA_START_ROW.

    In dcaLogSheet.js:
    (1) Extend `TX_HEADERS` (line 56) by appending `"Realized"` as the 10th column (col J).
    (2) Extend `SUMMARY_HEADERS` (lines 44-52) by appending `"Sold Qty"`, `"Net Proceeds"`,
        `"Realized $"`, `"Realized %"`, and `"Total Realized"` after the existing 7 BUY labels.
    (3) Add a `PERCENT_FORMAT = { type: "PERCENT", pattern: "0.00%" }` constant next to
        CURRENCY_FORMAT/DATE_FORMAT (copy the shape from dashboardSheet.js line 76).
    (4) Inside `bandRequests`, AFTER the existing BUY summary loop (do not touch lines 170-193),
        add a SECOND per-asset loop that emits the realized metrics via `formulaRowRequest` at
        startCol 8 (col H). Reuse the existing `dataAnchor`, `assetFilter` derivation; introduce
        `sellFilter = $C$${dataAnchor}:$C,"SELL"`. The four formulas (all wrapped IFERROR(...,"—")):
        Sold Qty = SUMIFS($E$..:$E,assetFilter,sellFilter); Net Proceeds =
        SUMIFS($F$..:$F,assetFilter,sellFilter) − SUMIFS($G$..:$G,assetFilter,sellFilter);
        Realized $ = SUMIFS($J$${dataAnchor}:$J,assetFilter,sellFilter) (sums the col J helper);
        Realized % = realizedDollars / (netProceeds − realizedDollars) (Pitfall 4 denominator —
        NEVER the current Avg Cost cell).
    (5) Emit the per-row realized spill via `formulaRowRequest(sheetId, TX_HEADER_ROW, 10,
        [realizedHeaderFormula])` — a SINGLE write to row 22 (TX_HEADER_ROW = DATA_START_ROW−1),
        col J. Build the formula string per RESEARCH lines 266-277: the whole-row
        `=BYROW(A${dataAnchor}:I${dataAnchor}, LAMBDA(r, LET(d,INDEX(r,1,1), ty,INDEX(r,1,3),
        q,INDEX(r,1,5), tot,INDEX(r,1,6), fee,INDEX(r,1,7), IF(d="","", IF(ty<>"SELL","",
        IFERROR((tot-fee) - q * ( SUMIFS(H${dataAnchor}:H,C${dataAnchor}:C,"BUY",
        A${dataAnchor}:A,"<="&d) / SUMIFS(E${dataAnchor}:E,C${dataAnchor}:C,"BUY",
        A${dataAnchor}:A,"<="&d) ),"—")))))` form. Use the WHOLE-row BYROW(A:I) construction
        (avoids the duplicate-date MATCH bug). All A23/I23 refs live ONLY inside the formula
        STRING; the request range is row 22 only.
    (6) Add a portfolio Total Realized cell: emit a single-cell formula (via formulaRowRequest)
        in the summary band (NOT the data region, NOT the Dashboard — D-03) summing the per-asset
        Realized $ summary column J over rows FIRST_SUMMARY_ROW..(1+assetList.length):
        =IFERROR(SUM($J$2:$J$${1+assetList.length}),"—"). Place it in a coherent summary cell
        (e.g. the "Total Realized" labeled column position) — document the exact cell in a comment.
    (7) Widen number formats: CURRENCY_FORMAT for Sold Qty/Net Proceeds/Realized $ summary
        columns (H=8, I=9, J=10), PERCENT_FORMAT for Realized % (K=11), each over
        FIRST_SUMMARY_ROW..LAST_RESERVED_SUMMARY_ROW (reservedEnd=21, endRowIndex 21 < 22).
        NEVER let any numberFormatRequest endRow reach >= DATA_START_ROW.
    Do NOT inline fenced code; follow the existing helper-call style. Do NOT add per-row writes
    to the data region — the spill is the ONLY mechanism touching rows >= 23, and it does so via
    a row-22 formula string, not a request range.
  </action>
  <verify>
    <automated>cd layout-builder && bun test src/dcaLogSheet.test.js 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `config.js` exports `DCA_LOG === "Transaction Log"` and `DCA_LOG_LEGACY === "DCA Log"`.
    - `JSON.stringify(dcaLogBuildRequests(0))` contains the substring `BYROW` exactly once and
      contains `,\"SELL\"` (escaped) for the realized summary SUMIFS.
    - The single `BYROW` spill formula is emitted by an `updateCells` whose
      `start.rowIndex === 21` (row 22, 0-based) and `start.columnIndex === 9` (col J, 0-based).
    - The existing test `summary formulas are BUY-only and use the em-dash empty state` still
      passes (BUY summary untouched — `,\"BUY\"` still present, IFERROR/—/SUMIFS/COUNTIFS/MAXIFS
      intact).
    - No new `numberFormatRequest`/`formulaRowRequest` range has `endRowIndex > 22` (will be
      enforced by the critical data-region test once Task 2 keeps it green).
    - `bun test src/dcaLogSheet.test.js` exits 0 after Task 2 updates the assertions (this task
      may leave the OLD conditional-format assertion failing until Task 2 — acceptable mid-plan).
  </acceptance_criteria>
  <done>
    config.js renamed + legacy constant added; dcaLogSheet.js emits the realized summary block,
    the row-22 BYROW spill, the portfolio total cell, and widened CURRENCY/PERCENT formats — all
    above DATA_START_ROW; the BUY-only summary loop is unmodified.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add conditional formatting to the log tab + update the test suite</name>
  <files>layout-builder/src/dcaLogSheet.js, layout-builder/src/dcaLogSheet.test.js</files>
  <read_first>
    - layout-builder/src/dashboardSheet.js lines 158-252 (conditional-format helper trio +
      GREEN_FILL/RED_FILL + MANAGED_RULE_COUNT + the build/update preClear gating in
      conditionalFormatRequests) and lines 449-461 (dashboardConditionalPreClearRequests — the
      exported split-batch pre-clear shape to mirror).
    - layout-builder/src/dcaLogSheet.js (FULL, AS MODIFIED BY TASK 1) — esp. bandRequests, the
      dcaLogBuildRequests/dcaLogUpdateRequests exports (lines 222-231), the new realized summary
      column positions from Task 1.
    - layout-builder/src/dcaLogSheet.test.js (FULL) — esp. lines 23-33 (EXPECTED_HEADERS),
      lines 64-79 (THE CRITICAL data-region assertion — keep green), lines 138-163 (the BUY/
      conditional-format assertions; lines 146-148 must be INVERTED), lines 165-167 (determinism),
      lines 174-269 (extractRanges + header-finder helpers — note extractRanges already handles
      addConditionalFormatRule ranges? confirm and extend if needed).
    - .planning/phases/06-realized-pnl-and-sell-log/06-PATTERNS.md lines 81-94 (conditional-format
      helper copy, Option A lowest churn) and lines 163-189 (test assertions to add/invert).
  </read_first>
  <action>
    In dcaLogSheet.js:
    (1) Copy the conditional-format helpers from dashboardSheet.js into dcaLogSheet.js (Option A,
        lowest churn — do NOT extract a shared module): `GREEN_FILL`, `RED_FILL`,
        `addConditionalFormatRuleRequest`, `deleteConditionalFormatRuleRequest`, and a
        log-tab-specific `DCA_LOG_MANAGED_RULE_COUNT = 2` (green Realized>0, red Realized<0 — no
        drift rule on this tab).
    (2) Give `bandRequests` a `preClearConditionalRules` parameter (default false). When true,
        emit `deleteConditionalFormatRuleRequest` for indices [DCA_LOG_MANAGED_RULE_COUNT−1..0]
        in DESCENDING order BEFORE the add rules (mirror dashboardSheet.js lines 216-220 gating
        rationale). When the param is false (build) emit NO deletes.
    (3) Add the two add rules over the Realized $ + Realized % summary cells. Build the range from
        the Task-1 realized summary column span: Realized $ = col J (0-based 9), Realized % = col K
        (0-based 10) → startColumnIndex 9, endColumnIndex 11; rows FIRST_SUMMARY_ROW−1 .. the last
        per-asset summary row (0-based: startRowIndex 1, endRowIndex 1+assetList.length). Conditions:
        NUMBER_GREATER 0 → GREEN_FILL (index 0), NUMBER_LESS 0 → RED_FILL (index 1). The range ends
        well above row 22 (summary band only) — no data-region concern.
    (4) Thread the param through the exports: `dcaLogBuildRequests` calls bandRequests(..., false);
        `dcaLogUpdateRequests` calls bandRequests(..., true).
    (5) Export `dcaLogConditionalPreClearRequests(sheetId)` returning the descending-index deletes
        [DCA_LOG_MANAGED_RULE_COUNT−1..0], mirroring dashboardConditionalPreClearRequests
        (dashboardSheet.js lines 455-461). Plan 02 (index.js) consumes this in its own
        error-tolerant pre-clear batch.

    In dcaLogSheet.test.js:
    (1) Add `"Realized"` to `EXPECTED_HEADERS` (now 10 columns). The header-finder helpers key off
        EXPECTED_HEADERS.length so they propagate automatically.
    (2) INVERT the assertion at lines 146-148: change `not.toContain("addConditionalFormatRule")`
        to `toContain("addConditionalFormatRule")` for both build and update; rename the test title
        to reflect that the log tab now HAS conditional formatting (D-07 reuse).
    (3) ADD tests (mirror existing shapes): (a) build contains the realized SELL summary —
        `expect(build).toContain(',\\"SELL\\"')`; (b) the BYROW spill is anchored at row 22 —
        find the updateCells whose formulaValue contains "BYROW" and assert its start.rowIndex===21
        and start.columnIndex===9; (c) Realized % uses IFERROR(...,"—"); (d) update emits the
        descending-index conditional-format DELETES (pre-clear) while build does NOT (mirror the
        Dashboard idempotency expectation).
    (4) Confirm `extractRanges` covers any range carried by addConditionalFormatRule; if a managed
        rule range is not already extracted, extend `extractRanges` so THE CRITICAL data-region
        assertion (lines 64-79) still sees every range. The conditional ranges end at
        1+assetList.length (<< 22), so the critical test must remain GREEN.
    Keep the determinism test (lines 165-167) green — build/update remain pure and deterministic.
  </action>
  <verify>
    <automated>cd layout-builder && bun test src/dcaLogSheet.test.js 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `bun test src/dcaLogSheet.test.js` exits 0 (all tests pass).
    - THE CRITICAL assertion ("NO dcaLogUpdateRequests range touches a row at or below the data
      region") passes — every range, including conditional-format ranges, has endRowIndex <= 22.
    - `JSON.stringify(dcaLogBuildRequests(0))` contains `addConditionalFormatRule`;
      `JSON.stringify(dcaLogUpdateRequests(0))` contains `addConditionalFormatRule` AND
      `deleteConditionalFormatRule` (build contains NO deleteConditionalFormatRule).
    - `dcaLogConditionalPreClearRequests(0)` returns an array of length 2 of
      deleteConditionalFormatRule requests in descending index order [1, 0].
    - `EXPECTED_HEADERS.length === 10` and the header-row test finds the 10-col header.
    - Determinism test passes (update twice == once).
  </acceptance_criteria>
  <done>
    Log tab emits 2 managed conditional-format rules (build: add-only; update: descending
    pre-clear + add), exports `dcaLogConditionalPreClearRequests`, and the full test suite passes
    including the unchanged critical data-region guard.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| layout-builder → Google Sheets API | Service-account-authenticated batchUpdate writes formulas/formats to the shared spreadsheet. Local-only, read-only against chains; the only write surface is the layout structure. |
| User-entered cells → realized formulas | The spill/summary formulas consume user-typed Total/Fee/Qty/Date/Type cells (untrusted shape: text in numeric cells, bad dates). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering / DoS | dcaLogSheet.js emitted request ranges | mitigate | Every new formula/format/conditional range stays endRowIndex <= 22; enforced by the existing critical data-region test (lines 64-79), which Task 2 keeps green and extends to cover conditional ranges. The row-22 BYROW spill is a single header-cell write — no per-row data-region updateCells. |
| T-06-02 | DoS (display) | realized spill + summary leaf cells | mitigate | `IF(d="","")` blank-row guard inside BYROW + `IFERROR(…,"—")` on every leaf; malformed user cells degrade to "—", never a #VALUE cascade. |
| T-06-03 | Tampering | BUY-only Phase 5 summary | accept→mitigate | The realized loop uses a strictly separate `Type="SELL"` filter and a new startCol; the BUY loop (lines 170-193) is not modified. The existing BUY-only test asserts the BUY filter survives. |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH "Package Legitimacy Audit": not applicable — all formula functions are Sheets built-ins; only existing `googleapis`). No slopcheck checkpoint needed. |
</threat_model>

<verification>
- `cd layout-builder && bun test src/dcaLogSheet.test.js` exits 0.
- `cd layout-builder && bun test` exits 0 (no regression in dashboard/other suites).
- Manual grep confirmation that the BUY summary loop (dcaLogSheet.js lines 170-193 region) is
  unchanged in shape and the realized loop is additive.
</verification>

<success_criteria>
- The Transaction Log builder emits per-row realized PnL (BYROW spill, row 22 only), per-asset
  realized summary metrics (Sold Qty / Net Proceeds / Realized $ / Realized %), a portfolio
  Total Realized cell, and green/red conditional formatting on the Realized cells.
- `DCA_LOG === "Transaction Log"`, `DCA_LOG_LEGACY === "DCA Log"`.
- The never-write-the-data-region guard and the BUY-only summary are provably intact (tests).
- `dcaLogConditionalPreClearRequests` is exported for Plan 02.
</success_criteria>

<output>
Create `.planning/phases/06-realized-pnl-and-sell-log/06-01-SUMMARY.md` when done.
</output>
