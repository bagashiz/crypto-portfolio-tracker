---
phase: 06-realized-pnl-and-sell-log
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - layout-builder/src/index.js
  - layout-builder/src/index.test.js
autonomous: true
requirements: [PNL-05]
must_haves:
  truths:
    - "Running --update on a spreadsheet whose log tab is still titled 'DCA Log' renames it in place to 'Transaction Log' without losing any data rows"
    - "Running --update on a spreadsheet already titled 'Transaction Log' renames nothing (idempotent) and still applies structure"
    - "The rename is an updateSheetProperties field-mask write (fields: 'title') — never a deleteSheet + addSheet"
    - "The log-tab conditional-format pre-clear runs in its own error-tolerant batch, mirroring the Dashboard pre-clear"
    - "The Apps Script runtime requires NO sheet-name change for the rename (verified no log-tab reference)"
  artifacts:
    - path: "layout-builder/src/index.js"
      provides: "Old-title-aware log-tab discovery + in-place rename in runUpdate; log-tab conditional pre-clear batch"
      contains: "DCA_LOG_LEGACY"
    - path: "layout-builder/src/index.test.js"
      provides: "Offline assertions for rename request shape, idempotent skip, never-delete-tab, pre-clear wiring"
      contains: "updateSheetProperties"
  key_links:
    - from: "index.js runUpdate"
      to: "config.js DCA_LOG_LEGACY"
      via: "tabs.get(DCA_LOG_LEGACY) fallback when DCA_LOG title absent"
      pattern: "DCA_LOG_LEGACY"
    - from: "index.js runUpdate"
      to: "dcaLogSheet.js dcaLogConditionalPreClearRequests"
      via: "separate error-tolerant batchUpdate before the structural batch"
      pattern: "dcaLogConditionalPreClearRequests"
---

<objective>
Wire the cross-runtime tab rename (D-07) into the layout-builder orchestrator: make `--update`
discover the log tab by the NEW title first, fall back to the LEGACY title, and rename it in
place via `updateSheetProperties(fields:"title")` — never delete+recreate. Also route the new
log-tab conditional-format pre-clear through its own error-tolerant batch (mirroring the
Dashboard), and record the verified Apps Script no-op.

Purpose: Complete D-07 so an existing "DCA Log" spreadsheet upgrades to "Transaction Log"
without data loss, and the Phase 6 conditional formatting applies idempotently.
Output: Extended `index.js` runUpdate (rename-aware discovery + log pre-clear batch) and offline
`index.test.js` assertions; a recorded verification that Apps Script needs no change.
</objective>

<execution_context>
@/home/bagashiz/Projects/crypto-portfolio-tracker/.claude/gsd-core/workflows/execute-plan.md
@/home/bagashiz/Projects/crypto-portfolio-tracker/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-realized-pnl-and-sell-log/06-CONTEXT.md
@.planning/phases/06-realized-pnl-and-sell-log/06-RESEARCH.md
@.planning/phases/06-realized-pnl-and-sell-log/06-PATTERNS.md
@.planning/phases/06-realized-pnl-and-sell-log/06-01-PLAN.md
</context>

<artifacts_this_phase_produces>
Symbols/behaviors introduced by THIS plan:

- `index.js`: imports `DCA_LOG_LEGACY` (and `dcaLogConditionalPreClearRequests`) added.
- `index.js runUpdate`: NEW old-title-aware log-tab discovery — `let logId = tabs.get(DCA_LOG);
  if undefined → tabs.get(DCA_LOG_LEGACY)`; on legacy hit, push an `updateSheetProperties`
  rename request (`{ properties: { sheetId: legacyId, title: DCA_LOG }, fields: "title" }`)
  into the structural batch and set `logId = legacyId`; if neither title found → throw.
- `index.js runUpdate`: NEW log-tab conditional pre-clear sent via the existing
  `isNoConditionalRuleAtIndexError` try/catch pattern, in its own batch (alongside / mirroring
  the existing `dashboardConditionalPreClearRequests` call).
- `index.test.js`: NEW offline unit-test file (or extension if one exists) asserting rename
  request shape, idempotent skip, never-delete-tab, and pre-clear wiring.
- A recorded NO-OP verification line in the SUMMARY: `apps-script/src/` references only
  `DASHBOARD_SHEET = "Dashboard"` (Refresh.ts:133/202); no log-tab reference → no Apps Script
  edit, no `bun build`, no `clasp push`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Verify Apps Script no-op, then add old-title-aware in-place rename + log pre-clear to runUpdate</name>
  <files>layout-builder/src/index.js</files>
  <read_first>
    - layout-builder/src/index.js (FULL) — esp. lines 23-30 (imports), lines 55-77
      (getExistingTabs title→sheetId map), lines 91-123 (runBuild — note it creates DCA_LOG with
      the NEW title via addSheet, leave build behavior as-is), lines 125-183 (runUpdate: discovery
      lines 137-150, the WR-01 error-tolerant Dashboard pre-clear lines 152-166 via
      isNoConditionalRuleAtIndexError, the structural batch lines 168-183).
    - layout-builder/src/dcaLogSheet.js lines 112-119 (freezeRowsRequest — the EXISTING
      updateSheetProperties field-mask idiom the rename mirrors) and the new
      `dcaLogConditionalPreClearRequests` export from Plan 01.
    - layout-builder/src/config.js — the `DCA_LOG` (now "Transaction Log") + `DCA_LOG_LEGACY`
      ("DCA Log") exports from Plan 01.
    - .planning/phases/06-realized-pnl-and-sell-log/06-RESEARCH.md lines 313-339 (In-place rename
      template), Pitfall 2 (lines 202-206), Pitfall 3 (lines 208-212: Apps Script no-op).
    - .planning/phases/06-realized-pnl-and-sell-log/06-PATTERNS.md lines 121-153 (tab discovery +
      field-mask idiom + the WR-01 split-batch pattern) and lines 192-212 (Apps Script verify-only).
  </read_first>
  <action>
    FIRST, run the verification grep over `apps-script/src/` for `getSheetByName` / `"DCA Log"` /
    `"Transaction Log"` to CONFIRM (per RESEARCH Pitfall 3 / PATTERNS lines 196-212) the only
    sheet-name reference is `DASHBOARD_SHEET = "Dashboard"` (Refresh.ts). If — and only if — a
    log-tab reference is found, STOP and surface it (it would require an Apps Script edit + rebuild
    + clasp push, out of this plan's scope). Expected result: no log-tab reference → record the
    no-op in the SUMMARY; make NO Apps Script edits, NO bun build, NO clasp push.

    Then edit index.js runUpdate:
    (1) Import `DCA_LOG_LEGACY` from config.js and `dcaLogConditionalPreClearRequests` from
        dcaLogSheet.js (add to the existing import lines 24/30).
    (2) Replace the current log-tab resolution (line 141 `const dcaLogId = tabs.get(DCA_LOG)`)
        with old-title-aware discovery: `let logId = tabs.get(DCA_LOG)` then, if undefined,
        `const legacyId = tabs.get(DCA_LOG_LEGACY)`. Keep the Dashboard discovery and the missing-
        tab error, but the log "missing" check must consider BOTH titles absent before throwing
        (`--update requires the log tab ("Transaction Log" or "DCA Log")`).
    (3) When the legacy title is found (new title absent), build a rename request mirroring
        freezeRowsRequest's field-mask shape: `{ updateSheetProperties: { properties: { sheetId:
        legacyId, title: DCA_LOG }, fields: "title" } }`, set `logId = legacyId`, and PREPEND this
        rename request to the structural `requests` array (so the rename lands before/with the
        structural re-apply, in the same atomic structural batch). Make it idempotent: if the NEW
        title already resolves, emit NO rename request (skip). NEVER emit deleteSheet/addSheet for
        the log tab on --update.
    (4) Add a log-tab conditional pre-clear mirroring the existing Dashboard one (lines 152-166):
        in a SEPARATE try/catch batch guarded by `isNoConditionalRuleAtIndexError`, call
        `dcaLogConditionalPreClearRequests(logId)`. IMPORTANT ordering: resolve/rename the log tab
        FIRST so `logId` is valid; the pre-clear targets the (possibly just-renamed) sheetId. You
        may combine the Dashboard + log pre-clears into the existing error-tolerant batch or use a
        second one — either is acceptable as long as a single out-of-range delete cannot roll back
        the structural batch (the whole point of WR-01). Keep the structural batch (the rename +
        dashboardUpdateRequests + dcaLogUpdateRequests) as the final atomic call.
    Update the runBuild path only if needed: build already creates the tab with the NEW title
    (config DCA_LOG) — no rename on build; leave runBuild's addSheet using DCA_LOG.
    No fenced code blocks in implementation; follow the existing index.js helper-call style.
  </action>
  <verify>
    <automated>grep -rn -i "getSheetByName\|\"DCA Log\"\|\"Transaction Log\"" apps-script/src/ | grep -v "Dashboard" | grep -vc "^#" </automated>
  </verify>
  <acceptance_criteria>
    - The grep over `apps-script/src/` returns NO log-tab reference (only `DASHBOARD_SHEET =
      "Dashboard"` at Refresh.ts:133/202) — the automated count of non-Dashboard log-tab refs is 0.
    - index.js imports `DCA_LOG_LEGACY` and `dcaLogConditionalPreClearRequests`.
    - runUpdate resolves the log tab by `DCA_LOG` then `DCA_LOG_LEGACY`, and the missing-tab error
      mentions BOTH titles.
    - When only the legacy title is present, runUpdate's request set contains exactly one
      `updateSheetProperties` with `fields: "title"` and `properties.title === "Transaction Log"`,
      and contains NO `deleteSheet`/`addSheet` for the log tab.
    - The log-tab conditional pre-clear is sent through the `isNoConditionalRuleAtIndexError`
      error-tolerant path (not the structural batch).
    - No Apps Script files modified; no `bun build` / `clasp push` performed.
  </acceptance_criteria>
  <done>
    Apps Script no-op confirmed and recorded; runUpdate renames the log tab in place (field-mask),
    idempotently skips when already renamed, never delete+recreates, and routes the log
    conditional pre-clear through the error-tolerant batch.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Offline tests for rename request shape, idempotency, and never-delete-tab</name>
  <files>layout-builder/src/index.test.js</files>
  <read_first>
    - layout-builder/src/index.js (FULL, AS MODIFIED BY TASK 1) — the runUpdate request assembly,
      the rename request shape, the discovery fallback.
    - layout-builder/src/dcaLogSheet.test.js lines 1-13 (test bootstrap: `import "./testEnv.js"`
      FIRST; bun:test import; GRID_ID pattern) and lines 169-269 (helper extraction style) — mirror
      the offline-pure assertion approach. If `index.test.js` does not yet exist, create it with the
      same bootstrap.
    - layout-builder/src/index.js getExistingTabs (lines 55-77) — to build a fake title→sheetId Map
      for the unit under test. Prefer extracting the pure request-assembly logic from runUpdate into
      a small exported helper (e.g. `buildLogTabRenameRequest(tabs)` and/or
      `resolveLogTabRequests(tabs)`) so it can be tested WITHOUT a live Sheets client — refactor
      Task 1's logic into a pure function if it is not already callable offline.
    - .planning/phases/06-realized-pnl-and-sell-log/06-RESEARCH.md Security Domain (lines 404-410:
      the "rename via delete+recreate" threat → explicit test that no addSheet/deleteSheet is
      emitted for the log tab on --update).
  </read_first>
  <action>
    Add (or create) `layout-builder/src/index.test.js` with `import "./testEnv.js";` FIRST, then
    bun:test. To keep tests offline (no Sheets client), have Task 1 expose the rename/discovery
    decision as a PURE function over a `Map<title, sheetId>` (e.g. a request-builder that returns
    the rename request array given the tabs map). Test cases:
    (1) Legacy-only map ({"DCA Log"→5, "Dashboard"→1}): the resolver returns a single
        updateSheetProperties rename request with fields "title", properties.sheetId===5,
        properties.title==="Transaction Log"; and resolves logId===5.
    (2) Already-renamed map ({"Transaction Log"→5, "Dashboard"→1}): the resolver returns NO rename
        request (idempotent skip) and resolves logId===5.
    (3) Neither-title map ({"Dashboard"→1}): the resolver THROWS an error mentioning both
        "Transaction Log" and "DCA Log".
    (4) Never-delete invariant: across cases (1) and (2), the returned request set contains NO
        `deleteSheet` and NO `addSheet` key for the log tab (assert via JSON.stringify not
        containing "deleteSheet"/"addSheet").
    (5) Rename request uses the data-preserving field mask: the rename request's `fields === "title"`.
    Keep tests pure/offline and deterministic (no network, no real client).
  </action>
  <verify>
    <automated>cd layout-builder && bun test src/index.test.js 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `cd layout-builder && bun test src/index.test.js` exits 0.
    - Tests assert: legacy→rename request (fields "title", correct sheetId/title); already-renamed→
      no rename request; neither→throws mentioning both titles; no deleteSheet/addSheet for the log
      tab in any --update path.
    - `cd layout-builder && bun test` exits 0 (full suite green — no regressions in
      dcaLogSheet.test.js or dashboard tests).
  </acceptance_criteria>
  <done>
    Offline tests prove the in-place rename is field-mask-based, idempotent, data-preserving (never
    delete+recreate), and errors clearly when the log tab is absent under both titles; full suite
    passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| layout-builder → Google Sheets API | runUpdate issues batchUpdate calls (rename + structure) as the service account against the shared spreadsheet. The rename mutates tab metadata, not cell data. |
| Existing spreadsheet state → orchestrator | The live tab may be titled "DCA Log" (pre-rename) or "Transaction Log" (post-rename); the orchestrator must handle both without data loss. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Tampering (irreversible data loss) | index.js log-tab rename | mitigate | Rename is `updateSheetProperties(fields:"title")` — field-mask semantics preserve all cell data. Task 2 asserts NO deleteSheet/addSheet for the log tab on --update. NEVER delete+recreate (D-07 / Phase 2 D-06). |
| T-06-05 | DoS (rollback) | conditional-format pre-clear on rule-count drift | mitigate | The log pre-clear runs in the existing `isNoConditionalRuleAtIndexError` error-tolerant batch (WR-01); an out-of-range delete cannot roll back the structural batch. |
| T-06-06 | Tampering (desync) | Apps Script log-tab reference | accept | Verified no-op: `apps-script/src/` references only `DASHBOARD_SHEET = "Dashboard"`; `refreshAll()` never touches the log tab, so the rename cannot desync the data layer. No Apps Script change needed. |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH audit not applicable — existing `googleapis` only). |
</threat_model>

<verification>
- `cd layout-builder && bun test` exits 0 (full suite: index.test.js, dcaLogSheet.test.js,
  dashboard tests all green).
- `grep -rn -i "getSheetByName|\"DCA Log\"|\"Transaction Log\"" apps-script/src/` shows only the
  Dashboard reference — recorded as the D-07 Apps Script no-op.
- runUpdate request assembly contains exactly one field-mask rename request when given a
  legacy-titled tab, and none when given an already-renamed tab.
</verification>

<success_criteria>
- `--update` upgrades an existing "DCA Log" tab to "Transaction Log" in place with zero data loss
  and is idempotent on reruns.
- The rename is a field-mask `updateSheetProperties`, never delete+recreate (test-enforced).
- The log-tab conditional pre-clear is isolated in the error-tolerant batch.
- The Apps Script side is confirmed and recorded as a no-op (no edit/build/push).
</success_criteria>

<output>
Create `.planning/phases/06-realized-pnl-and-sell-log/06-02-SUMMARY.md` when done.
</output>
